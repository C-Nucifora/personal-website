/**
 * GENERATED by scripts/fetch-projects.mjs — do not edit.
 * Synced from github.com/C-Nucifora; curate in data/projects-config.mjs
 * and run `npm run fetch-projects`.
 */
export interface GithubCommit {
  hash: string;
  date: string;
  message: string;
}

export interface GithubProject {
  slug: string;
  title: string;
  pitch: string;
  stack: string[];
  sourceUrl: string;
  liveUrl?: string;
  featured: boolean;
  stars: number;
  pushedAt: string;
  readme?: string;
  commits: GithubCommit[];
}

export const githubProjects: GithubProject[] = [
  {
    "slug": "m1-core",
    "title": "m1-core",
    "pitch": "Core M1 language library (AST, parser, evaluator)",
    "stack": [
      "Rust",
      "ast",
      "language-tooling",
      "m1-script",
      "motec"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-core",
    "featured": true,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-core\n\nShared foundation for the MoTeC M1 (`.m1scr`) tooling: CST helpers, diagnostics,\nand (later) a `.m1prj`/`.m1cfg` symbol model. Downstream tools (`m1-fmt`,\n`m1-lint`, `m1-typecheck`, `m1-lsp`) depend on this crate and never import\ntree-sitter directly.\n\n## Layering: type inference lives in `m1-typecheck` (#8)\n\n`m1-core` deliberately exposes only *structural* CST access — `kind`, `children`,\n`field`, `byte_range`, and friends — and **no type-query API on `Node`**. Asking\n\"what type does this expression resolve to?\" requires the project symbol model\n(`.m1prj`/`.m1cfg`), which lives in the layer **above** this crate, in\n`m1-typecheck`. Putting an `infer_type(node, …)` here would invert that\ndependency. So type information has a single canonical home: downstream tools get\nit from `m1-typecheck` — `resolve(path, scope)` and `typer::type_of(node, scope)`\n— rather than from `m1-core`. (Resolution of #8, option (a).)\n\n## Workspace layout\n\nThe M1 toolchain lives in **six separate repositories** coupled through Cargo\n**path** dependencies. They are not published to crates.io, so this crate does\n**not** build from a standalone clone — check out the whole set as siblings under\none parent directory:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root)\n├── m1-core/          # this crate\n├── m1-lint/          # depends on ../m1-core\n├── m1-fmt/           # depends on ../m1-core\n├── m1-typecheck/     # depends on ../m1-core\n└── m1-lsp/           # depends on the four above\n```\n\n**`m1-core` depends on `../tree-sitter-m1`** (`tree-sitter-m1 = { path =\n\"../tree-sitter-m1\" }`) and generates `src/kind.rs` / `src/field.rs` from that\ncrate's `node-types.json` — so a clean build, and the `kind_rs_is_fresh` /\n`field_rs_is_fresh` tests, require the matching `tree-sitter-m1` checked out\nalongside it. It is in turn depended on by `m1-lint`, `m1-fmt`, `m1-typecheck`,\nand `m1-lsp`.\n\nBecause the repos are independent on GitHub, this coupling is **not visible\nthere**: each repo's CI and PRs see only itself. Build/merge ordering across the\nstack is a manual, local-workspace concern.\n\n## v1 surface\n\n```rust\nlet cst = m1_core::parse(src);\nfor node in cst.root().children() {\n    match node.kind() {\n        m1_core::Kind::IfStatement => { /* ... */ }\n        _ => {}\n    }\n}\nlet diagnostics = cst.syntax_diagnostics(); // Vec<m1_core::Diagnostic>\n```\n\n## Annotations (`// @m1:<kind>(args)`)\n\nComment-embedded attributes — the M1 analogue of Rust attributes / `// eslint-disable` —\nparsed once here and consumed by every downstream tool. They ride inside ordinary\n`//` (and `/* */`) comments, so they are valid M1 and need no grammar change.\n\n```rust\n// // @m1:allow(L010, T030)   ← suppress L010/T030 on the following statement\n// Front Torque = 1; // @m1:safety-critical   ← trailing form attaches to this statement\nlet reg = m1_core::Registry::seed();           // or build your own with only the kinds you consume\nlet anns = m1_core::annotations(&cst, &reg);\nfor a in anns.all() { /* a.kind, a.args, a.target_byte_range */ }\nlet warnings = anns.diagnostics();             // unknown-kind warnings (Code::Annotation)\nlet suppressed = anns.is_allowed(\"L010\", byte_offset); // honour @allow when filtering diagnostics\n```\n\nAn annotation attaches to a **construct**: a comment trailing a statement on the\nsame line attaches to that statement; otherwise it is *leading* and attaches to\nthe next statement (so annotations stack on consecutive lines above their target).\nA tool registers the kinds it owns; m1-core emits a `Severity::Warning`\n(`Code::Annotation`) for any `@m1:` kind not in the registry — an unknown attribute.\n\n## Codegen\n\n`src/kind.rs` is generated from `tree-sitter-m1`'s `node-types.json`. After a\ngrammar change, regenerate it:\n\n    cargo run -p xtask -- gen-kinds\n\nA test (`xtask`'s `kind_rs_is_fresh`) fails if the committed file is stale.\n\n## Test\n\n    cargo test\n\n## Known limitations (v1)\n\nThese are deliberate v1 scoping/behavior choices, tracked for later increments:\n\n- **No symbol model yet.** `.m1prj`/`.m1cfg` loading, name resolution, and types\n  are deferred until `m1-typecheck` drives their shape.\n- **Diagnostic double-reporting.** `syntax_diagnostics()` emits a `MissingToken`\n  for a MISSING node *in addition to* the `SyntaxError` for its enclosing ERROR\n  node, so a single mistake can surface as two overlapping diagnostics. Acceptable\n  for v1; revisit before wiring diagnostics into `m1-lsp`.\n- **Generic ERROR message.** ERROR nodes report `\"syntax error\"` without the\n  offending token text.\n- **Iterative tree walk.** `syntax_diagnostics()` traverses with an explicit\n  work-stack, so even pathologically deep input cannot overflow the call stack (#28).\n- **Byte-column positions.** `Position::column` is a byte offset; UTF-16/LSP\n  position conversion is the responsibility of `m1-lsp`.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "123ded4",
        "date": "2026-06-11",
        "message": "Register the fmt annotation kind in Registry::seed() (#45)"
      },
      {
        "hash": "8a8dce8",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog + consume/install docs (#43)"
      },
      {
        "hash": "f93f0df",
        "date": "2026-06-09",
        "message": "feat: tree-sitter-m1 v0.5.0 — IsPatternList kind + Pattern field (#42)"
      },
      {
        "hash": "aed82c1",
        "date": "2026-06-09",
        "message": "chore(deps): bump tree-sitter-m1 in the m1-toolchain group (#41)"
      },
      {
        "hash": "3bc1525",
        "date": "2026-06-08",
        "message": "fix(annotation): respect nested parens in arg-list scanning (#40)"
      },
      {
        "hash": "e25623d",
        "date": "2026-06-07",
        "message": "feat: shared Kind operator predicates + byte_to_position; split annotation args (#39)"
      },
      {
        "hash": "f8dc69d",
        "date": "2026-06-07",
        "message": "test(corpus): assert non-empty instead of EV-sized >= 80 floor (#38)"
      },
      {
        "hash": "1a11f62",
        "date": "2026-06-05",
        "message": "fix(cst): char-boundary-safe point_at + iterative max_depth guard (#37)"
      }
    ]
  },
  {
    "slug": "m1-lsp",
    "title": "m1-lsp",
    "pitch": "Language server for M1 Script",
    "stack": [
      "Rust",
      "language-server",
      "language-tooling",
      "lsp",
      "m1-script"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-lsp",
    "featured": true,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-lsp\n\nA [Language Server Protocol](https://microsoft.github.io/language-server-protocol/)\nserver for the MoTeC M1 script language (`.m1scr`). It is the integration layer\nat the top of the M1 toolchain: it parses files through `m1-core`, surfaces\nsyntax and lint diagnostics live, and formats documents through `m1-fmt`.\n\nThe primary target editor is **Neovim**, but the server speaks plain LSP over\nstdio and works with any LSP client (VS Code, Helix, Emacs `eglot`, etc.).\n\n## Workspace layout\n\n`m1-lsp` sits at the **top** of the M1 toolchain, which lives in **six separate\nrepositories**. They are not published to crates.io; instead each crate pins its\nupstreams as **versioned git-tag Cargo dependencies**, so this crate **does**\nbuild from a standalone clone — Cargo fetches every upstream from its tagged\nrelease. Checking the whole set out as siblings under one parent directory is\nhandy for cross-repo work, but is not required to build:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root)\n├── m1-core/          # parse / CST / diagnostics\n├── m1-lint/          # linter\n├── m1-fmt/           # formatter\n├── m1-typecheck/     # type checker\n└── m1-lsp/           # this crate\n```\n\n**`m1-lsp` depends on all four upstream crates** —\n`m1-core`, `m1-lint`, `m1-fmt`, and `m1-typecheck` (each a git-tag dep), plus\n`tree-sitter-m1` and `m1-workspace` — so a clean build of `m1-lsp` against the\npinned upstream tags is the toolchain's end-to-end integration check.\n\nBecause every dependency is pinned by tag, the coupling **is** visible on\nGitHub — each `Cargo.toml` names its upstreams and their versions, and Dependabot\nopens bump PRs as new upstream tags ship. Cutting a new upstream release and\nbumping `tag = \"vX.Y.Z\"` in each consumer is what propagates a change across the\nstack. The `m1-example` example project (used by the corpus smoke test) is an\noptional sibling checkout.\n\n## Features\n\n- **Diagnostics** (`textDocument/publishDiagnostics`): the union of\n  - `m1-core` syntax diagnostics (source `m1-core`),\n  - `m1-lint` rule findings `L001`–`L009` (source `m1-lint`),\n  - `m1-typecheck` type diagnostics `T0xx` (source `m1-typecheck`, requires a\n    loaded project), with deprecated-overload findings tagged\n    `DiagnosticTag::Deprecated`, and\n  - an `unsupported-c-token` check (source `m1-intrinsics`) that flags C\n    operators M1 rejects (`==`/`!=`/`&&`/`||`/`!`/`while`/`for`/`do`).\n\n  Recomputed on open / change / save, and re-published when the project model\n  reloads.\n\n  **Project-scope diagnostics** (source `m1-typecheck`) — the `.m1cfg`-coverage\n  audit (`T041`: a parameter declared in the `.m1prj` but missing from the\n  `.m1cfg`) and the name/component audit (`T050`/`T010`/`T071`) — are published\n  against the `Project.m1prj` URI once the project loads, so the editor matches\n  what the CLI reports rather than only showing per-script findings.\n- **Quick-fixes** (`textDocument/codeAction`): for the fixable\n  `unsupported-c-token` operators, a quick-fix replaces them with the M1\n  keyword (`==`→`eq`, `!=`→`neq`, `&&`→`and`, `||`→`or`, `!`→`not`); for a\n  `T020` enum-non-member, a \"did you mean\" fix replaces the typo with the nearest\n  valid enum member (e.g. `Drive State.Of`→`Drive State.Off`). A whole-file\n  **Fix all auto-fixable lint issues** action (`source.fixAll`) applies the shared\n  `m1-lint` fixer, covering every fixable rule (L002/L003/L007/L011/L018, …) so\n  editors can clean a file on save.\n- **Formatting** (`textDocument/formatting`): whole-document reformat via\n  `m1-fmt`, returned as a single full-document `TextEdit`. No edits are returned\n  when the document is already formatted or has syntax errors (pass-through\n  safety).\n- **Hover** (`textDocument/hover`): type / kind of the symbol under the cursor —\n  locals (inferred type), project channels/parameters/constants (with units and\n  named enum types), and library functions (signatures, `stateful` /\n  `deprecated` flags).\n- **Go-to-definition** (`textDocument/definition`): jumps to the backing\n  `.m1scr` / `.m1dbc` file of a project function or DBC signal, the `.m1prj`\n  declaration line of a project channel/parameter, or — for a bare `local` — its\n  `local …` declaration in the same file (works with no project loaded).\n- **References, implementation & document highlights** (`textDocument/references`,\n  `textDocument/implementation`, `textDocument/documentHighlight`): occurrences of\n  the local / channel / symbol under the cursor — project-wide for channels,\n  file-local for locals — with read/write classification for highlights, and\n  `implementation` keeping only the write (producer) sites. Channel occurrences are\n  matched by **canonical path** (resolved through each script's group scope), so the\n  same channel written group-relative in one script and read full-path in another\n  collapses onto one entity instead of splitting by spelling.\n- **Document symbols** (`textDocument/documentSymbol`): outline of locals,\n  assignment targets, and call statements (e.g. `Output.SetState`), nested under\n  their `if`/`when` blocks — so call-only actuator/fault scripts still get an\n  outline.\n- **Completion** (`textDocument/completion`): in-scope locals, project symbols,\n  library objects and keywords; after a library object `.` (a trigger\n  character), that object's methods. Project symbols carry their value type and\n  unit in the completion `detail` (e.g. `Unsigned · ratio`, `Enum (Drive State)`).\n- **Signature help** (`textDocument/signatureHelp`): library-function overloads\n  with the active argument highlighted (triggered on `(` and `,`), the\n  name + return type of a user function/method, and the parameter list of a\n  project-object method call (`Channel.Set`, `Table.Lookup`, `X.AsInteger`, …).\n- **Inlay hints** (`textDocument/inlayHint`): an inline `: Type` after each\n  un-annotated `local`.\n- **Rename** (`textDocument/rename` + `prepareRename`): file-scoped rename of a\n  `local` (project symbols are declared in `.m1prj` and are not renamed here).\n- **Folding** (`textDocument/foldingRange`): `{ … }` blocks and multi-line block\n  comments.\n- **Code lens** (`textDocument/codeLens`): a `⚡ N Hz` lens at the top of each\n  `.m1scr` naming the script's execution rate, derived from its `.m1prj`\n  `SelectedTrigger` clock (#86, view half). Shown only when the rate is\n  statically known; startup-only and `$(…)`-templated triggers carry no lens.\n  Changing the rate writes to `Project.m1prj` and lives in the editor/CLI layer,\n  not the server.\n- **Semantic tokens** (`textDocument/semanticTokens/full`): full-document token\n  classification (variables, functions, keywords, numbers, strings, comments,\n  types, parameters, namespaces; `definition` / `readonly` modifiers).\n- **Call hierarchy** (`textDocument/prepareCallHierarchy` +\n  `callHierarchy/incomingCalls` / `outgoingCalls`): the cross-script channel\n  *data-flow* graph (#84). An item is a channel or a `.m1scr` script. From a\n  channel, **incoming** lists the scripts that read it and **outgoing** the\n  scripts that write (produce) it; from a script, **outgoing** lists the channels\n  it writes and **incoming** the scripts that read a channel it writes. Channel\n  references are resolved through each script's group scope, so the same channel\n  written/read under different group-relative spellings collapses onto one node.\n  Script items show their call rate (e.g. `Engine.Control @ 100 Hz`).\n- **Position encoding**: byte offsets from `m1-core` are converted to LSP\n  positions in UTF-16 code units by default, or UTF-8 when the client negotiates\n  it.\n- **Document lifecycle**: full-document sync (`didOpen` / `didChange` /\n  `didSave` / `didClose`), plus `workspace/didChangeWatchedFiles` for\n  `.m1prj` / `.m1cfg` reloads.\n\nThe project model auto-discovers a `parameters.m1cfg` in the project directory\nor an ancestor (nearest wins) and loads it via `m1-typecheck`; this is what gives parameters\ntheir concrete **value types and units** (the `.m1prj` mostly just names them).\nThose types flow into hover, completion detail, inlay hints, and assignment\ntype-checking, and the server reloads them when the `.m1cfg` changes.\n\n## Build\n\n```bash\ncargo build --release      # binary at target/release/m1-lsp\n# or:\ncargo install --path .     # installs `m1-lsp` onto your $PATH\n```\n\nRun with no arguments to start the language server over stdio (how editors launch\nit). `m1-lsp --help` / `--version` print and exit; `--scaffold-config` prints a\ndefault `m1-tools.toml`. An unrecognised flag exits non-zero rather than blocking.\n\n## Editor setup\n\nSee [`editors/nvim/README.md`](editors/nvim/README.md) for Neovim setup,\ncovering both the `m1-lsp` server (diagnostics + formatting) and\n`tree-sitter-m1` highlighting / indent / fold via `nvim-treesitter`.\n\n## Architecture\n\n- `src/line_index.rs` — encoding-aware byte ↔ LSP position conversion.\n- `src/convert.rs` — `m1-core` / `m1-lint` diagnostics → `lsp-types`.\n- `src/analysis.rs` — `analyze()`: unions syntax diagnostics with a\n  `LintProvider`.\n- `src/format.rs` — `Formatter` trait + full-range `TextEdit` construction.\n- `src/lint_backend.rs` / `src/fmt_backend.rs` — the real `m1-lint` / `m1-fmt`\n  backends.\n- `src/backend.rs` — the `tower-lsp` `LanguageServer` implementation.\n\n`m1-lsp` never imports `tree-sitter` directly; all CST access is via `m1-core`.\n\n## Testing\n\n```bash\ncargo test                 # unit + integration + corpus smoke test\nM1_CORPUS_PATH=/path cargo test --test corpus   # point the smoke test elsewhere\n```\n\nThe corpus smoke test runs `analyze()` over every `.m1scr` in\n`$M1_CORPUS_PATH` (falling back to the sibling m1-example example project) and\nasserts it never panics. It is skipped if the directory is absent.\n\n## Releases\n\n`.github/workflows/release.yml` publishes prebuilt server binaries to a GitHub\nRelease (`v<crate-version>`) for **Linux x64**, **Windows x64**, and\n**Apple-Silicon macOS (arm64)**. These are consumed by the `m1-vscode` extension.\n\n### Intel macOS\n\nIntel macOS (`x86_64-apple-darwin`) binaries are **not** published — GitHub no\nlonger reliably provides Intel-Mac CI runners. On an Intel Mac, build the server\nyourself:\n\n```bash\ngit clone https://github.com/C-Nucifora/m1-lsp && cd m1-lsp\ncargo build --release            # -> target/release/m1-lsp\n```\n\nThen point the editor at it (in VS Code: set `m1.server.path` to that binary, or\nput `m1-lsp` on your `PATH`). See the `m1-vscode` README for the full Intel-Mac\nsetup.\n\n## Status\n\nv2. The symbol-model features (hover, completion, go-to-definition, signature\nhelp, inlay hints, semantic tokens, and type diagnostics) are implemented on top\nof the `m1-typecheck` project model, alongside references, document highlights,\nfolding, rename, and code-action quick-fixes.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "c719232",
        "date": "2026-06-11",
        "message": "chore: bump m1-typecheck to v0.33.0 (case-insensitive enum heads); release v0.35.0 (#248)"
      },
      {
        "hash": "f04c2bf",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog, install/verify docs, SHA256SUMS (#247)"
      },
      {
        "hash": "0215352",
        "date": "2026-06-10",
        "message": "feat: class help summaries in object hover; bump m1-typecheck to v0.32.0 (#246)"
      },
      {
        "hash": "af3db0c",
        "date": "2026-06-10",
        "message": "chore: bump m1-typecheck to v0.31.1, version to 0.33.3 (#245)"
      },
      {
        "hash": "b3980cc",
        "date": "2026-06-10",
        "message": "chore: bump m1-typecheck to v0.31.0, version to 0.33.2 (#244)"
      },
      {
        "hash": "29157ab",
        "date": "2026-06-10",
        "message": "chore: bump m1-typecheck to v0.30.1, version to 0.33.1 (#243)"
      },
      {
        "hash": "f5ef374",
        "date": "2026-06-10",
        "message": "chore: bump version to 0.33.0 (#242)"
      },
      {
        "hash": "2d89bf6",
        "date": "2026-06-10",
        "message": "Surface T095 invalid-display-unit (Error 1017) in the LSP (#241)"
      }
    ]
  },
  {
    "slug": "nvim-m1",
    "title": "nvim-m1",
    "pitch": "Neovim plugin for M1 script: LSP, tree-sitter, formatting, and linting in one setup() call",
    "stack": [
      "Lua",
      "language-tooling",
      "lsp",
      "m1-script",
      "motec"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/nvim-m1",
    "featured": true,
    "stars": 1,
    "pushedAt": "2026-06-11",
    "readme": "# nvim-m1\n\nNeovim plugin for [M1 script](https://github.com/C-Nucifora/m1-tools) (`.m1scr`). Provides LSP, tree-sitter highlighting, format-on-save, and standalone linting in a single `setup()` call — the Neovim equivalent of [m1-vscode](https://github.com/nedlane/m1-vscode).\n\n## Requirements\n\n- Neovim ≥ 0.10\n- [lazy.nvim](https://github.com/folke/lazy.nvim) (or any plugin manager)\n- [tree-sitter-m1](https://github.com/C-Nucifora/tree-sitter-m1) — the `m1` grammar + queries (a dependency, pulled in automatically)\n- A C compiler (`cc`/`gcc`/`clang`) on `$PATH` — the parser is compiled from tree-sitter-m1's sources on first setup\n- On Neovim 0.10: [nvim-lspconfig](https://github.com/neovim/nvim-lspconfig) (0.11+ uses the native LSP API)\n- Optional: [conform.nvim](https://github.com/stevearc/conform.nvim), [nvim-lint](https://github.com/mfussenegger/nvim-lint)\n- `curl` on `$PATH` — used once to download the bundled toolchain\n- macOS only: `codesign` (ships with the Xcode Command Line Tools, alongside the C compiler\n  above) — the downloaded binaries are re-signed ad-hoc on install so Apple Silicon's\n  code-signing check doesn't kill them\n\n**The M1 toolchain is bundled** — you don't install `m1-lsp`/`m1-fmt`/`m1-lint`/`m1-project`\nyourself. The lazy.nvim `build` hook below downloads the pinned, prebuilt binaries for your\nplatform into `stdpath(\"data\")/nvim-m1/bin` on install/update (re-signing them ad-hoc on\nmacOS so they run); `:M1Install` / `:M1Update` do it\non demand. A binary you put on `$PATH` (or point to via `server_path`/`project_path`) still wins.\n`m1-lsp` alone provides diagnostics, hover, completion, formatting and rename — it embeds\nm1-fmt/m1-lint/m1-typecheck — so the LSP is the only hard requirement; the rest enable the\nconform.nvim / nvim-lint / project-editing paths. Run `:checkhealth nvim-m1` to verify.\n\nTree-sitter highlighting works through Neovim core: nvim-m1 compiles the `m1` parser from\ntree-sitter-m1's sources into a site `parser/m1.so` and registers its queries directly, so\nit does **not** depend on a particular nvim-treesitter branch (the `main` rewrite dropped the\nruntime `:TSInstall`/`install_info` path nvim-m1 used to rely on). On Neovim 0.11+ the server\nis registered with the native `vim.lsp.config`/`vim.lsp.enable` API; on 0.10 it falls back to\nnvim-lspconfig. Format-on-save uses conform.nvim when present and otherwise falls back to LSP\nformatting; standalone lint uses nvim-lint when present and otherwise a built-in runner.\n\n## Installation\n\n```lua\n-- lazy.nvim\n{\n  \"C-Nucifora/nvim-m1\",\n  dependencies = {\n    \"C-Nucifora/tree-sitter-m1\", -- the m1 grammar + queries (required)\n    { \"nvim-treesitter/nvim-treesitter\", optional = true },\n    { \"neovim/nvim-lspconfig\", optional = true }, -- only needed on Neovim 0.10\n    { \"stevearc/conform.nvim\", optional = true },\n    { \"mfussenegger/nvim-lint\", optional = true },\n  },\n  -- Downloads the bundled M1 toolchain (m1-lsp/fmt/lint/project) for your\n  -- platform on install + update. Same as running :M1Install.\n  build = function()\n    require(\"nvim-m1.install\").install()\n  end,\n  ft = { \"m1scr\", \"m1prj\" },\n  opts = {\n    -- Path to the m1-lsp binary (default: search $PATH for \"m1-lsp\")\n    server_path = nil,\n    -- Format .m1scr buffers on save (m1-fmt via conform.nvim, else LSP)\n    format_on_save = true,\n    -- Lint .m1scr buffers on save (m1-lint via nvim-lint, else built-in runner)\n    lint_on_save = true,\n  },\n}\n```\n\n### Options\n\n| Key | Default | Description |\n| --- | --- | --- |\n| `server_path` | `nil` | Path to the m1-lsp binary; `nil` searches `$PATH`. |\n| `project_path` | `nil` | Path to the m1-project binary (Project.m1prj editor; powers `:M1CreateChannel` etc.); `nil` searches `$PATH`. |\n| `format_on_save` | `true` | Format `.m1scr` on write with m1-fmt. |\n| `lint_on_save` | `true` | Lint `.m1scr` on write with m1-lint. |\n| `filetypes` | `{ \"m1scr\" }` | Script filetypes to wire. |\n| `attach_m1prj` | `true` | Also attach m1-lsp to `Project.m1prj` (rename a channel from its declaration). |\n| `root_markers` | `{ \"Project.m1prj\", \".git\" }` | Files marking a project root. |\n| `auto_install_parser` | `true` | Run `:TSInstall m1` if the parser is missing. |\n| `lint_on_insert_leave` | `false` | Also lint on `InsertLeave`. |\n| `codelens` | `true` | Show m1-lsp code lenses (e.g. a script's `⚡ N Hz` rate) and keep them refreshed; run the lens under the cursor with `:M1CodeLensRun`. |\n| `capabilities` / `on_attach` | — | Forwarded to the LSP client. |\n| `settings` | `{}` | Unified m1-lsp config (lint/format/diagnostics), e.g. `{ lint = { max_line_length = 100 }, diagnostics = { ignore = { \"T041\" } } }`. A workspace `m1-tools.toml` overrides it. |\n\n### Configuration\n\n`settings` is the convenient per-setup config. For **project-level** config shared\nwith teammates (and the VS Code extension), commit an `m1-tools.toml` to the project\nroot — the server discovers and applies it, **overriding** `settings`. It configures\nthe same lint thresholds, formatter options, and cross-source diagnostic\n`ignore`/`select` (any lint `L*` or typecheck `T*` code). Generate one pre-filled\nwith all defaults via `:M1GenerateConfig`.\n\n### Commands\n\n| Command | Action |\n| --- | --- |\n| `:M1Format` | Format the current buffer now. |\n| `:M1FormatToggle` | Toggle format-on-save for this session. |\n| `:M1Lint` | Lint the current buffer now. |\n| `:M1GenerateConfig` | Write a default `m1-tools.toml` to the project root. |\n| `:M1CreateChannel` | Create a channel in `Project.m1prj` (prompts for name/type/unit/security). |\n| `:M1SetSecurity` | Set a component's security/access level. |\n| `:M1SetType / :M1SetUnit / :M1SetCallRate` | Set a script's execution rate (picked from the project's clocks). |\n| `:M1CreateGroup` | Create a group in `Project.m1prj` (prompts for the fully-qualified name). |\n| `:M1RenameComponent` | Rename a component (picked from the project) and update its trigger references; warns when a backing `.m1scr` file may need renaming too. |\n| `:M1DeleteComponent` | Delete a component (picked from the project, confirms first; optional subtree). |\n| `:M1ValidateProject` | Validate `Project.m1prj` structure into the quickfix list. |\n| `:M1Install` / `:M1Update` | Download the bundled M1 toolchain at the pinned versions. |\n| `:checkhealth nvim-m1` | Verify Neovim version, toolchain binaries, parser and integrations. |\n\nThe project-editing commands drive `Project.m1prj` through the [`m1-project`](https://github.com/nedlane/m1-project)\nbinary (the same tool the VS Code extension uses) — the language server stays\nread-only and reloads automatically after an edit. Put `m1-project` on `$PATH`\n(or set `project_path`); `:checkhealth nvim-m1` reports whether it's found.\n\n## Features\n\n| Feature | Provider |\n| --- | --- |\n| Syntax highlighting | tree-sitter-m1 |\n| Diagnostics (syntax + lint + types) | m1-lsp |\n| Hover, completion, go-to-definition | m1-lsp |\n| Go-to-implementation (a channel's write/producer sites) | m1-lsp |\n| Find references, rename | m1-lsp |\n| Inlay type-hints | m1-lsp |\n| Semantic tokens | m1-lsp |\n| Format-on-save | conform.nvim + m1-fmt |\n| Standalone lint | nvim-lint + m1-lint |\n\n## Development\n\n```sh\nscripts/test.sh   # headless plenary-busted suite\n```\n\nThe suite covers config resolution, the m1-lint JSON parser, setup wiring, and an\nend-to-end lint run against the real `m1-lint` binary (when it is on `$PATH`). Test\nfixtures are synthetic — no project data is checked in.\n\n## License\n\nGPL-3.0-or-later — see [LICENSE](LICENSE).\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n\n## Statusline component\n\n`require(\"nvim-m1.statusline\").component` returns `\"m1 v<server-version>\"`\nwhen the m1-lsp client is attached to the current M1 buffer, `\"m1 ✗\"` when it\nis not, and `\"\"` in non-M1 buffers. Drop it into lualine:\n\n```lua\nsections = { lualine_x = { require(\"nvim-m1.statusline\").component } }\n```\n\n## which-key labels\n\nWith [which-key](https://github.com/folke/which-key.nvim) installed:\n\n```lua\nrequire(\"nvim-m1.whichkey\").register()                    -- <leader>m…\nrequire(\"nvim-m1.whichkey\").register({ prefix = \"<leader>k\" })\n```\n\nregisters labelled bindings for every `:M1*` command (a silent no-op when\nwhich-key is absent).\n",
    "commits": [
      {
        "hash": "9e110cb",
        "date": "2026-06-11",
        "message": "chore: bundle m1-lsp v0.35.0; release v0.8.5 (#60)"
      },
      {
        "hash": "e0f8d0f",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog + consume/install docs (#59)"
      },
      {
        "hash": "768793b",
        "date": "2026-06-10",
        "message": "chore: bundle m1-lsp v0.34.0; release v0.8.4 (#58)"
      },
      {
        "hash": "6c08677",
        "date": "2026-06-10",
        "message": "chore: bump bundled m1-lsp to v0.33.3, release v0.8.3 (#57)"
      },
      {
        "hash": "6f2c707",
        "date": "2026-06-10",
        "message": "chore: bump bundled m1-lsp to v0.33.2, release v0.8.2 (#56)"
      },
      {
        "hash": "14380af",
        "date": "2026-06-10",
        "message": "chore: bump bundled m1-lsp to v0.33.1, release v0.8.1 (#55)"
      },
      {
        "hash": "15bd2c9",
        "date": "2026-06-10",
        "message": "chore: bump bundled toolchain (m1-lsp v0.33.0, m1-project v0.4.0); v0.8.0 (#54)"
      },
      {
        "hash": "12a3c7a",
        "date": "2026-06-10",
        "message": "chore: bundle m1-lsp v0.32.0 / m1-lint v0.14.0 / m1-project v0.3.1 (#53)"
      }
    ]
  },
  {
    "slug": "tree-sitter-m1",
    "title": "tree-sitter-m1",
    "pitch": "Tree-sitter grammar for M1 Script",
    "stack": [
      "C",
      "grammar",
      "language-tooling",
      "m1-script",
      "motec"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/tree-sitter-m1",
    "featured": true,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# tree-sitter-m1\n\nA [tree-sitter](https://tree-sitter.github.io/) grammar for the **MoTeC M1\nscript language** (`.m1scr`) — the C#-like language used inside MoTeC M1 Build\nto program M1-series ECUs (e.g. the M150).\n\nThis is the **foundation** of the M1 editor-tooling stack. It produces:\n\n- a parser the Rust tools consume (`m1-core`, `m1-lint`, `m1-fmt`,\n  `m1-typecheck`, `m1-lsp`), and\n- highlight / indent / fold queries for Neovim.\n\n## The hard part: identifiers contain spaces\n\nIn M1, a path segment can contain spaces and `.` separates segments:\n\n```\nBrenloft.Quassor.Vund Klee.Mosko.Trilby Glonk = CanComms.GetUnsignedInteger(h, 48, 16);\nPellow.KVB Bonquil eq Wexlar Bonquil Mosko.Vor\n```\n\n> **Note:** All example identifiers in this repo (here, in the grammar/scanner\n> comments, and in the corpus tests) are synthetic placeholders. The grammar and\n> tests were anonymised; the names are not drawn from any real project.\n\nA regex token can't express \"a run of words, but stop before the keyword `eq`\".\nSo the `identifier` token is produced by an **external scanner**\n([`src/scanner.c`](src/scanner.c)) that joins `word SPACE word` greedily while\nrefusing to absorb reserved words. The reserved set there must stay in sync with\nthe keyword strings in [`grammar.js`](grammar.js).\n\n## Compile-time interpolation: `$(VAR)`\n\nA standalone `$(VAR)` used as an operand parses as its own `interpolation`\nnode:\n\n```\nx = $(SEG) + 1;   // value is (binary_expression (interpolation) (number))\n```\n\nWhen `$(VAR)` instead leads a multi-word name it stays inside the\n`identifier` segment, preserving \"one identifier = one path segment\":\n\n```\nnaxID Bnk $(SEG) Vlim = 1;   // target is a single (identifier)\n```\n\n(Example names are synthetic placeholders, not from any real project.)\n\n## Layout\n\n| Path | Purpose |\n|---|---|\n| `grammar.js` | Grammar definition |\n| `src/scanner.c` | External scanner (space-joined identifiers) |\n| `src/parser.c` | Generated parser (committed so the Rust crate builds without the CLI) |\n| `queries/*.scm` | highlight / indent / fold / injection queries |\n| `bindings/rust/` | Rust crate exposing `LANGUAGE` + query strings |\n| `test/corpus/` | tree-sitter corpus tests covering the grammar's constructs |\n\n## The toolchain workspace\n\nThe M1 toolchain lives in **six separate repositories** that depend on each other\nthrough **versioned git-tag** Cargo dependencies. They are not published to\ncrates.io, but each crate still builds from a standalone single-repo clone —\nCargo fetches its upstreams from their tagged releases. Cloning the whole set as\nsiblings under one parent directory is handy for cross-repo work, not required to\nbuild:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root of the dependency graph) — this repo\n├── m1-core/          # parse / CST / diagnostics; depends on tree-sitter-m1\n├── m1-lint/          # linter;          depends on m1-core\n├── m1-fmt/           # formatter;       depends on m1-core\n├── m1-typecheck/     # type checker;    depends on m1-core\n└── m1-lsp/           # language server; depends on the four above\n```\n\n`tree-sitter-m1` is the **root**: it has no sibling dependencies and builds on its\nown. All five Rust crates depend on it (directly or transitively) via a\n**versioned git-tag** Cargo dep, e.g.\n`tree-sitter-m1 = { git = \"https://github.com/C-Nucifora/tree-sitter-m1.git\", tag = \"v0.5.0\"  # pin the latest release }`.\nIn particular `m1-core` regenerates its `Kind` enum from this crate's\n`node-types.json`, so a grammar change here ripples downstream.\n\nBecause every consumer pins this crate by tag, the coupling **is** visible on\nGitHub — each `Cargo.toml` names the tag it depends on, and Dependabot opens bump\nPRs as new tags ship. Cutting a new release here and bumping `tag = \"vX.Y.Z\"` in\nthe consumers is what propagates a grammar change across the stack. The\n`m1-example` example project (used by some corpus tests) is an optional sibling\ncheckout.\n\n## Develop\n\n```bash\nnpm install                 # gets the tree-sitter CLI locally\nnpx tree-sitter generate    # regenerate src/parser.c from grammar.js\nnpx tree-sitter test        # run corpus tests\n# acceptance gate: parse every real corpus script, fail on any ERROR/MISSING\nscripts/check-corpus.sh\n# parse a real script from the sibling m1-example repo:\nnpx tree-sitter parse \"../m1-example/UQR-EV/01.00/Scripts/CAN.DBC Init.m1scr\"\n```\n\nUse as a Rust dependency:\n\n```toml\n[dependencies]\ntree-sitter = \"0.25\"\ntree-sitter-m1 = { git = \"https://github.com/C-Nucifora/tree-sitter-m1.git\", tag = \"v0.5.0\"  # pin the latest release }\n```\n\n```rust\nlet mut parser = tree_sitter::Parser::new();\nparser.set_language(&tree_sitter_m1::LANGUAGE.into())?;\n```\n\n## Neovim setup\n\n> **Want the full M1 experience?** Use the unified\n> [nvim-m1](https://github.com/C-Nucifora/nvim-m1) plugin, which wires this\n> grammar together with `m1-lsp`, `m1-fmt`, and `m1-lint` behind a single\n> `setup` call. The spec below installs **only this grammar** — use it if you\n> want syntax highlighting / indent / folds without the rest of the toolchain.\n\nRegister the parser config before installing so nvim-treesitter recognises the language (avoids the `skipping unsupported language: m1` warning):\n\n```lua\n{\n    \"C-Nucifora/tree-sitter-m1\",\n    dependencies = { \"nvim-treesitter/nvim-treesitter\" },\n    config = function()\n        -- Map the `.m1scr` filetype to the `m1` tree-sitter language.\n        vim.filetype.add({ extension = { m1scr = \"m1scr\" } })\n        vim.treesitter.language.register(\"m1\", \"m1scr\")\n\n        -- Register the parser. The nvim-treesitter rewrite returns the config\n        -- table directly; legacy builds expose get_parser_configs().\n        local parsers = require(\"nvim-treesitter.parsers\")\n        local parser_config = type(parsers.get_parser_configs) == \"function\"\n                and parsers.get_parser_configs()\n            or parsers\n        parser_config.m1 = {\n            install_info = {\n                url = \"https://github.com/C-Nucifora/tree-sitter-m1\",\n                -- The grammar has an external scanner, so scanner.c is required.\n                files = { \"src/parser.c\", \"src/scanner.c\" },\n                branch = \"main\",\n            },\n            filetype = \"m1scr\",\n        }\n        vim.cmd(\"TSInstall! m1\")\n    end,\n}\n```\n\n## Status\n\nStable and complete against both real corpora and the M1 Build Development\nManual's operator/construct tables: every script in the EV-M1 and AV-M1\ncorpora parses with zero `ERROR`/`MISSING` nodes (`scripts/check-corpus.sh` is\nthe CI gate). This grammar is the foundation of the wider M1 toolchain —\n`m1-core`, `m1-fmt`, `m1-lint`, `m1-typecheck`, `m1-lsp`, editor plugins —\nlaid out as sibling repos via the\n[`m1-tools`](https://github.com/C-Nucifora/m1-tools) vcstool manifest, which is\nthe up-to-date map of the ecosystem.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "a2bbb74",
        "date": "2026-06-11",
        "message": "Retire stale PLAN.md / STATUS.md (#48)"
      },
      {
        "hash": "6be6e60",
        "date": "2026-06-11",
        "message": "Ship queries/textobjects.scm (nvim-treesitter-textobjects) (#47)"
      },
      {
        "hash": "2ff8db1",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog + consume/install docs (#43)"
      },
      {
        "hash": "bc86584",
        "date": "2026-06-09",
        "message": "is_pattern_list node, reference-keyword highlights, PLAN closeouts (#42)"
      },
      {
        "hash": "1c32930",
        "date": "2026-06-08",
        "message": "fix(queries): fold and indent the `when` block, not just its `is` arms (#38)"
      },
      {
        "hash": "be3c8f1",
        "date": "2026-06-07",
        "message": "chore: fix operator highlights, sync version, doc hygiene (#37)"
      },
      {
        "hash": "bf32d90",
        "date": "2026-06-06",
        "message": "docs: recommend nvim-m1; genericize corpus path; drop UQR-EV (#30)"
      },
      {
        "hash": "5a7a19d",
        "date": "2026-06-05",
        "message": "fix(scanner): bound `$(` interpolation scan to kill O(n²) parse DoS (#36)"
      }
    ]
  },
  {
    "slug": "telescope-m1.nvim",
    "title": "telescope-m1.nvim",
    "pitch": "Telescope extension for M1 script: workspace symbols, component browser, lint rule picker",
    "stack": [
      "Lua",
      "language-tooling",
      "m1-script",
      "motec",
      "neovim"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/telescope-m1.nvim",
    "featured": false,
    "stars": 1,
    "pushedAt": "2026-06-11",
    "readme": "# telescope-m1.nvim\n\n[Telescope](https://github.com/nvim-telescope/telescope.nvim) extension for [M1 script](https://github.com/C-Nucifora/m1-tools). Adds pickers for:\n\n- **Workspace symbols** — fuzzy-search all channels, parameters, enums and functions in the loaded project\n- **Component browser** — browse the project's component tree as an indented hierarchy\n- **Lint rules** — pick an `m1-lint` rule to open its docs, yank its code, or ignore it\n\nThe symbol and component pickers are powered by `m1-lsp`'s `workspace/symbol` —\nthe component browser presents the *same* data the toolchain builds from\n`Project.m1prj`, so it never drifts from the project. The lint-rule list is kept\nin sync with `m1-lint` (see [Staying in sync](#staying-in-sync)).\n\n## Requirements\n\n- Neovim ≥ 0.10\n- [telescope.nvim](https://github.com/nvim-telescope/telescope.nvim)\n- [m1-lsp](https://github.com/C-Nucifora/m1-lsp) running in the workspace (symbol/component pickers)\n- Recommended: [nvim-m1](https://github.com/C-Nucifora/nvim-m1) — sets up `m1-lsp` and shares the\n  canonical client name so the pickers find it automatically. Works with any\n  setup that runs `m1-lsp`, but nvim-m1 is the turn-key path.\n\n## Installation\n\n```lua\n-- lazy.nvim\n{\n  \"C-Nucifora/telescope-m1.nvim\",\n  dependencies = {\n    \"nvim-telescope/telescope.nvim\",\n    \"C-Nucifora/nvim-m1\", -- recommended: starts m1-lsp + shares the client name\n  },\n  config = function()\n    require(\"telescope\").load_extension(\"m1\")\n  end,\n}\n```\n\n## Usage\n\n```lua\n-- Workspace symbol picker\nrequire(\"telescope\").extensions.m1.workspace_symbols()\n\n-- Project component browser\nrequire(\"telescope\").extensions.m1.components()\n\n-- Lint rule picker\nrequire(\"telescope\").extensions.m1.lint_rules()\n```\n\nOr via command palette:\n```\n:Telescope m1 workspace_symbols\n:Telescope m1 components\n:Telescope m1 lint_rules\n:Telescope m1 call_rates\n```\n\n### Picker mappings\n\n| Picker | Key | Action |\n| --- | --- | --- |\n| workspace_symbols / components | `<CR>` | jump to the symbol's definition |\n| components | `<C-f>` | (functions) jump to the backing script |\n| components | `<C-s>` / `<C-t>` / `<C-u>` | set the entry's security / storage type / display unit (via nvim-m1 + m1-project) |\n| call_rates | `<CR>` | browse the scripts scheduled at the picked rate |\n| call_rates | `<C-a>` | assign a script to the picked rate |\n| lint_rules | `<CR>` | open the rule's documentation |\n| lint_rules | `<C-y>` | yank the rule code (e.g. `L004`) |\n| lint_rules | `<C-i>` | append the code to the project's `.m1lint.toml` ignore list |\n\n## Staying in sync\n\nThis extension deliberately avoids re-implementing what the toolchain already\nowns, so new M1 features show up here without code changes:\n\n- **Symbols & components** come from `m1-lsp` (`workspace/symbol`). There is no\n  separate `.m1prj` parser to fall out of date.\n- **The lint-rule catalogue** is sourced from `m1-lint`: a test runs\n  `m1-lint --rules --format json` and fails if `lua/telescope-m1/rules.lua`\n  drifts from the binary (codes, names, fixability).\n- **The LSP client name** is read from `nvim-m1` when present, so the two plugins\n  can never disagree about which client to talk to.\n\n## Development\n\n```sh\nscripts/test.sh   # headless plenary-busted suite\n```\n\nTests use synthetic fixtures only — no project data is checked in.\n\n## License\n\nGPL-3.0-or-later — see [LICENSE](LICENSE).\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "204d1af",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog + consume/install docs (#12)"
      },
      {
        "hash": "ac8e281",
        "date": "2026-06-11",
        "message": "fix: add L024 ternary-condition-parens + L025 local-scope-too-wide to rule catalogue (#13)"
      },
      {
        "hash": "df72344",
        "date": "2026-06-09",
        "message": "feat: components picker actions, call_rates picker, L020-L023 catalogue (#11)"
      },
      {
        "hash": "6013536",
        "date": "2026-06-08",
        "message": "fix: don't pick a non-M1 LSP as the m1-lsp client (#7)"
      },
      {
        "hash": "04b6e39",
        "date": "2026-06-08",
        "message": "fix: merge into existing ignore array instead of duplicating the key (#6)"
      },
      {
        "hash": "9348d40",
        "date": "2026-06-07",
        "message": "fix: sync lint rule catalogue with linter, run sync test against bundled binary (#5)"
      },
      {
        "hash": "d0144a9",
        "date": "2026-06-03",
        "message": "docs: add MoTeC trademark / non-affiliation disclaimer (#3)"
      },
      {
        "hash": "9cb6ac0",
        "date": "2026-06-03",
        "message": "fix: sync rule catalogue with m1-lint (L010 name, add L014–L017) (#4)"
      }
    ]
  },
  {
    "slug": "m1-typecheck",
    "title": "m1-typecheck",
    "pitch": "Neovim type-checker plugin for M1 Script (via nvim-lint)",
    "stack": [
      "Rust",
      "language-tooling",
      "m1-script",
      "motec",
      "neovim"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-typecheck",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-typecheck\n\nThe semantic-analysis layer of the MoTeC M1 (`.m1scr`) toolchain. Where `m1-core`\n(syntax) and `m1-lint` (CST-only style rules) know nothing about what a name *is*,\n`m1-typecheck` loads the project's **symbol model** and uses it to resolve\nidentifiers, type expressions, and emit type/naming diagnostics that are\nimpossible without that knowledge.\n\nIt is both a **library** (consumed by `m1-lsp` for hover/types and as a third\ndiagnostic source) and a **CLI**.\n\n## Workspace layout\n\nThe M1 toolchain lives in **six separate repositories**. They are not published\nto crates.io; instead each crate pins its upstreams as **versioned git-tag Cargo\ndependencies** (e.g. `m1-core = { git = \"…/m1-core.git\", tag = \"v0.3.1\" }`), so\nthis crate **does** build from a standalone clone — Cargo fetches its upstreams\nfrom their tagged releases. Checking the whole set out as siblings under one\nparent directory is handy for cross-repo work, but is not required to build:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root)\n├── m1-core/          # parse / CST / diagnostics\n├── m1-lint/          # linter\n├── m1-fmt/           # formatter\n├── m1-typecheck/     # this crate\n└── m1-lsp/           # language server; depends on the four above\n```\n\n**`m1-typecheck` depends on `m1-core` and `m1-workspace`** (git-tag deps;\n`m1-core` transitively pulls in `tree-sitter-m1`), and is itself a git-tag\ndependency of `m1-lsp`. (The `m1-example` example project, used by the corpus\ngate, is an optional sibling checkout — see the env-var notes below.)\n\nBecause every dependency is pinned by tag, the coupling **is** visible on\nGitHub — each `Cargo.toml` names its upstreams and their versions, and\nDependabot opens bump PRs as new upstream tags ship. Cutting a new upstream\nrelease and bumping `tag = \"vX.Y.Z\"` in each consumer is what propagates a\nchange across the stack.\n\n## The symbol model\n\n`m1-typecheck` parses the project's MoTeC `Project.m1prj` (XML) into a flat\n`SymbolTable` keyed by fully-qualified dotted path (`Root.Foo.Bar`). Each symbol\ncarries a `SymbolKind` (Channel, Parameter, Constant, Function, Method, Table,\nGroup, Reference, Other), derived from the component `Classname`. Enum data types\n(`<DataTypes>`) become `EnumType`s.\n\n### `parameters.m1cfg` — the parameter type source\n\nThe `.m1prj` names parameters but rarely says what *type* they hold. The sibling\n**`parameters.m1cfg`** is where that lives: each `<Parameter><Cell Type=\"…\"\nUnit=\"…\">` gives a parameter its concrete **value type** (`f32`→`Float`,\n`u16`→`Unsigned`, `s16`→`Integer`, `bool`→`Boolean`, `enum`→a specific enum type)\nand its **unit**. Loading it is what turns parameters from untyped names into\ntyped symbols, which is what powers:\n\n- assignment type-mismatch checking (`T030`) against parameter targets,\n- enum association of `enum`-typed cells (see below),\n- richer hover/completion in the language server (type + unit).\n\nIt is **auto-discovered**: the first `*.m1cfg` found by searching the project\nfile's directory and its ancestors (nearest wins) is loaded automatically (the\nCLI, the language server, and therefore m1-ci all do this), so a project that\nships a `parameters.m1cfg` gets type-aware checking with no extra configuration.\n`--config` overrides the discovered file.\n\n`FuncUser`/`MethodUser` components carry a `Filename`, mapping each script to its\n**enclosing group** — the base for relative name resolution.\n\n### Name resolution\n\nA reference resolves as: local → absolute (and `Root.`-prefixed) → group-relative\n(walking ancestor groups) → built-in/opaque root. Unknown and library roots are\ntreated as **opaque** (never flagged); an accessor on a resolved channel/parameter\nor value-bearing compound (e.g. `SomeChannel.AsInteger`) is also opaque. Only a\npath rooted in a known *namespace* project group whose leaf is absent is reported\nunresolved — this is the false-positive guard.\n\n### Type model\n\n`ValueType` is `Boolean | Integer | Unsigned | Float | Enum | String | Unknown`.\n`Unknown` is absorbing and silent: a rule fires only when all operands it needs\nare *known*, eliminating false positives from gaps in the symbol data. Locals are\ntyped by Hungarian prefix (`b`/`u`/`i`/`f` + UpperCase).\n\n## Rules\n\n| Code | Severity | Rule |\n|------|----------|------|\n| T001 | Warning | **unresolved-reference** — member path whose root is a known project group does not resolve. |\n| T002 | Error | **float-equality** — `==`/`!=`/`eq`/`neq` with a `Float` operand; use a tolerance check. |\n| T003 | Warning | **float→integer narrowing** — a compound assignment (`+=` `-=` `*=` `/=`) storing a `Float` value back into an integral target, which truncates. Plain integer/float *widening* in arithmetic is implicit in M1 Build and not flagged. |\n\n## v2: enum domain model, flow analysis, and the project audit\n\nv2 builds a live **enum domain model** on top of the v1 symbol table:\n\n- A **member index** (member name → enum type ids) is built once at load, with\n  queries `enum_by_name`, `enums_with_member`, `enum_has_member`, `enum_type`.\n- **Enum association**: a `.m1cfg` parameter cell `<Cell Type=\"enum\">…</Cell>`\n  back-resolves its member value to a *unique* enum type and associates the\n  parameter with it (zero or many candidates → left `Unknown`, silent).\n- The **typer** produces `ValueType::Enum(id)` for a typed member path\n  `<EnumTypeName>.<Member>` (when the head names an enum that declares the member)\n  and for any enum-associated symbol.\n\nThis unlocks four new rules, added to the default rule set\n(`Registry::default()`) that `check_script` runs:\n\n| Code | Severity | Rule |\n|------|----------|------|\n| T020 | Warning | **enum-non-member** — a typed member path `<EnumType>.<Member>` whose member the enum does not declare. |\n| T021 | Warning | **enum-numeric-comparison** — a comparison with one *known* `Enum` operand and the other *known* numeric. |\n| T004 | Warning | **signed-unsigned-comparison** — an ordering comparison (`<` `>` `<=` `>=`) mixing a known `Unsigned` operand with a known signed `Integer`, where the result can flip on wraparound. Narrow by design: ordering only, and literal operands are allowed. |\n| T030 | Warning | **assignment-type-mismatch** — `target = value` where the target is a Channel/Parameter with a known type and the value's known type is incompatible. |\n| T031 | Warning | **unresolved-assignment-target** — the target of a plain `=` assignment is a project-rooted path that does not resolve (e.g. `Root.Foo.Missing = 1`). Distinct from T001 because a write target is not a read; compound assignments (`+=`, …) read the target first and stay T001. |\n| T040 | Warning | **channel-multiple-assignment** — a Channel assigned more than once on a single code path (control-flow aware: mutually-exclusive `if`/`else` arms are not a conflict). Implemented as a whole-tree pass in `flow.rs`, not a node-at-a-time rule. |\n| T070 | Error | **when-is-exhaustive** — a `when (subject) { is (…) … }` over an enum-typed subject that does not cover every enumerator (`or` groups enumerators within one arm). One of the remaining §6 compile-time checks M1 Build enforces. An arm naming a non-member label is treated as a catch-all (the `when` is then exhaustive); a non-enum subject is T082's finding. |\n| T080 | Error / Warning | **invalid-value-reaches-finite-sink** — NaN/Inf provenance tracing (see below). Fires only for a value marked `@m1:requires-finite` / `@m1:safety-critical`; otherwise silent. |\n| T085 | Error / Warning | **user-argument-mismatch** — a call to a user function whose `.m1prj` `<Signature>` declares parameters: wrong arity is an Error, an incompatible argument type a Warning. `In.<Param>` reads inside the callee are typed from the same signature. |\n| T086 | Warning | **unit-mismatch** — a direct channel-to-channel copy whose two sides carry different physical quantities (base units from `Props Qty`). Computed right-hand sides are unchecked (arithmetic legitimately changes dimension). |\n| T088 | Warning (opt-in) | **circular-dependency** — same-rate write/read cycles between scripts (manual p.29). Opt-in: real projects contain deliberate same-rate feedback loops M1 Build accepts. |\n| T089 | Hint (opt-in) | **rate-inversion** — a faster script reads a channel written only at a slower rate (stale between writer ticks). Opt-in: often intentional downsampling. |\n| T082 | Error | **when-subject-not-enum** — the manual (p.32) requires the `when` argument to be an enumerated data type; fires when the subject's type is *known* and non-enum. |\n| T083 | Error | **static-local-initialiser** — a `static local`'s initial value must be a literal, enumerator or constant (manual p.34); runtime reads and calls flag. Constant-foldable literal arithmetic (`2 * 60`) is accepted. |\n| T084 | Error | **expand-bounds** — `expand` bounds must be literals or constants, 0 or positive (manual p.33); negative, float and runtime-value bounds flag. |\n| T087 | Warning | **type-restricted-to-locals** — project audit: a Channel/Parameter declared `bool`/`string`, which the manual (p.24) restricts to local variables. CAN/DBC signals are exempt. |\n| T091 | Warning | **local-object-case-ambiguity** — a `local` whose name matches the leaf of a project object referenced in the same script, identically or differing only by case (manual pp.64–65: \"don't distinguish local variable names from other object names only by uppercase/lowercase writing\"). Package-object internals are exempt. |\n| T092 | Warning (opt-in) | **untagged-component** — M1 Build tag-warning parity (manual p.67): a user Channel/Parameter whose effective tag set (own `SelectedTags` ∪ inherited group tags) has no System tag (Engine/Vehicle/Driver) and/or no Type tag (Normal/Diagnostic/Advanced/Pin/Tune/Setup); one finding per missing group. Opt-in: the real corpora use no tags at all. |\n\nEvery v2 rule keeps the v1 invariant: it fires only when every type/enum it needs\nis **known**, and stays silent under `Unknown` — so the m1-example corpus stays free of\nspurious diagnostics.\n\n### T080 invalid-value (NaN/Inf) provenance tracing\n\nM1 floats are IEEE 754 binary32 with **no \"no-data\" sentinel**, so an invalid\nresult is a genuine NaN/±Inf that propagates silently (or, scaled to an int for\nCAN/logging, reads as an extreme \"legitimate\" value). The type lattice can't see\nthis. **T080** tracks a *can-be-invalid* bit and flags an unsanitised invalid\nvalue reaching a finiteness sink.\n\nIt is **annotation-driven** (zero-noise unless opted in):\n\n```m1\n// @m1:source(volatile)         <- a sensor channel that can drop out → NaN\nGyro Z = Sbg.Read();\n\n// @m1:safety-critical          <- this value MUST stay finite (error if not)\nFront Axle Torque = Yaw Moment / Track Width;   // T080: division can be NaN/Inf\n```\n\n- **Sources** (can-be-invalid): division, domain math (`Calculate.FastSquareRoot`,\n  `Math.Log`, …), explicit `Calculate.Infinity()`, and reads of an\n  `@m1:source`/`@m1:external` channel.\n- **Sanitiser** (clears the bit): an assignment annotated `@m1:sanitizes` /\n  `@m1:clears`. Note `Math.IsNaN()` is **calibration-only** per the Development\n  Manual, so it cannot guard an ECU script. **Clamps do _not_ sanitise** —\n  `Limit.Range`/`Calculate.Min`/`Calculate.Max` pass NaN straight through (NaN\n  defeats min/max).\n- **Latching escalation:** a tainted value reaching a stateful function\n  (`Filter.*`/`Integral.*`/`Derivative.*`) is escalated to an **error** — the\n  filter state stays poisoned after the input recovers.\n\nSeverity: `@m1:safety-critical` → error (fails the build); `@m1:requires-finite`\n→ warning, escalated to error when latched. Because m1-typecheck exits non-zero\non any error, this **is** the CI gate — no separate wiring.\n\n#### Cross-script propagation\n\nThe analysis is **project-wide**: every run with a project solves the\nwriter→reader channel graph over all the scripts passed on the command line\n(pass the whole project, as CI does, so the solve sees every edge). A channel\nwritten from an invalid-value source in one script taints its reads everywhere\nelse, through any depth of copies — the solve iterates to a **fixpoint** over a\nfinite monotone lattice, so even feedback loops between scripts terminate. An\n`Out =` assignment writes the script's backing function symbol (the same\n`Filename=`/path convention used for return-type inference), and a call to that\nfunction reads it, so taint also flows through user-function results.\nDiagnostics carry the full backward provenance chain:\n\n```\nwarning[T080]: a value required to be finite can be NaN/Inf: `Root.Demo.Heading` is\nwritten by `Demo.Update.m1scr` from `Root.Demo.Rate`; `Root.Demo.Rate` is written by\n`Demo.Read.m1scr`: division (the denominator may be 0) — guard it with Math.IsNaN()\nor a finite fallback\n```\n\n`@m1:sanitizes`/`@m1:clears` is the cross-script barrier too (a sanitised\nrewrite stops the chain), latching escalates across scripts the same way, and\n**T081** also fires cross-script when a remotely-tainted float is stored into an\ninteger channel. Function argument→`In` flow is not yet modelled (only `Out` and\nchannel reads propagate). Query one channel's solved status with\n[`--explain`](#cli-usage).\n\n### T050 project-name audit (opt-in)\n\n`--audit-names` runs a project-wide symbol-name audit (`Project::audit()`),\nchecking each symbol's leaf name against the m1-example naming conventions: Channels and\nFunctions/Methods `lowerCamelCase`, Parameters/Groups and enum types\n`UpperCamelCase`, Constants `CAPITALISATION` (spaces allowed). It emits **T050**\ndiagnostics at the project level (no source line/col). It is **off by default**:\nthe example project massively violates these conventions, so running it as part of\nthe per-script CI flow would be pure noise. The corpus gate records only a baseline\ncount for it.\n\nThe same audit also emits **T071 name-case-collision** (Warning, the other\nremaining §6 compile-time check): (a) any two project symbols whose fully-qualified\ndotted paths are equal case-insensitively but differ in actual case (reported once\nper colliding pair), and (b) any project symbol whose leaf name case-insensitively\nmatches a firmware library object name (`Calculate`, `CanComms`, …), which it would\nshadow. Both flow through `Project::audit()` / `--audit-names` alongside T050.\n\nThe audit also emits **T010 invalid-component-parent** (Warning): a component\nwhose `Classname` is only valid under a specific parent class but whose actual\nparent has a different class. The class schema (`classname.rs`) is deliberately\nconservative — only classes with an unambiguous nesting invariant are constrained\n(today the CAN frame hierarchy: a `BuiltIn.CAN.Signal` must sit under a\n`BuiltIn.CAN.Message`, a `BuiltIn.CAN.Message` under a `BuiltIn.CAN.DBC`), so\nwell-formed projects never flag. Every component's raw `Classname` is stored on\n`Symbol::classname`, ready for richer class-aware checks and completion.\n\n## Suppressing a diagnostic — `// @m1:allow(...)`\n\nA `// @m1:allow(T0xx, …)` annotation (the toolchain annotation framework,\nm1-core#33) suppresses the listed checks on the construct it attaches to; a bare\n`// @m1:allow` suppresses every check there.\n\n```m1\nlocal b = 1.5;\nlocal c = 2.5;\n// @m1:allow(T002)\nif (b == c) { }            // T002 (float ==) not reported here\n```\n\nIt attaches **leading** (the next statement, stacking on consecutive lines) or\n**trailing** (a statement it follows on the same line); suppression is\nline-scoped to the target construct. Project-level findings (T010/T041/T050/T071/T087/T092, not tied\nto a source construct) are **not** suppressible this way — quiet a whole code\nwith `--ignore` / the `[diagnostics]` config, or suppress a single symbol with\n`[diagnostics] ignore_symbols = [\"T050:Root.Engine.Speed\"]` in `m1-tools.toml`\n(or the `--ignore-symbol` flag).\n\n## CLI usage\n\n```\nm1-typecheck [--project <Project.m1prj>] [--config <parameters.m1cfg>]\n             [--audit-names] [--select <CODES>] [--ignore <CODES>] [--ignore-symbol <CODE:PATH>]\n             [--format human|json] [--rules] [--explain <CHANNEL>] <file.m1scr>...\n```\n\n```sh\nm1-typecheck --project Project.m1prj --config parameters.m1cfg Scripts/*.m1scr\nm1-typecheck --project Project.m1prj --audit-names\nm1-typecheck --ignore T041 Scripts/*.m1scr        # silence the cfg-coverage audit\nm1-typecheck --audit-names --ignore-symbol T050:Root.GPS  # allow one naming exception\nm1-typecheck --format json Scripts/*.m1scr        # machine-parsable diagnostics\nm1-typecheck --select T064 Scripts/*.m1scr        # opt into wrong-argument-count\nm1-typecheck --select T088,T089 Scripts/*.m1scr   # scheduling audit (cycles + rate inversions)\nm1-typecheck --select T092 Scripts/*.m1scr        # M1 Build tag-warning parity (untagged components)\nm1-typecheck --explain-units \"Driveline.Accumulator.Voltage\" Scripts/*.m1scr\nm1-typecheck --rules                              # list every T-code\nm1-typecheck --explain Demo.Rate Scripts/*.m1scr  # trace a channel's NaN provenance\n```\n\n- `--project` defaults to the nearest `Project.m1prj` upward from the first input\n  file, or `$M1_PROJECT`. With no project found, runs in project-less mode (T001\n  disabled).\n- `--config` defaults to the first `*.m1cfg` found by searching the project\n  file's directory and its ancestors (nearest wins), or `$M1_CONFIG`; pass it\n  explicitly to override. Without a cfg, parameters are untyped (`Unknown`) and\n  type-dependent rules stay silent for them.\n- When both a project **and** a `.m1cfg` are loaded, the run also emits **T041**\n  (`missing-cfg-parameter`) once per `BuiltIn.Parameter` that the `.m1prj`\n  declares but the `.m1cfg` omits — M1-Build falls back to its default value for\n  those. Hint severity (#156): riding the default is normal, supported M1\n  behaviour — most parameters in a real project intentionally stay uncalibrated\n  — so at hundreds per healthy project a Warning would bury every actionable\n  diagnostic. Teams that require full calibration coverage can still surface the\n  set via `--select T041` or `m1-cfg-export --missing-only` (same set).\n- `.m1dbc` files under the project directory are auto-loaded, so CAN signals\n  resolve. **T042** (`dbc-signal-range`) then flags a **literal** value assigned\n  to a signal that falls outside its physical range (derived from the signal's\n  `Length`/`Type` raw range scaled by `Multiplier`/`Offset`). Computed RHS values\n  are left alone (unknown at check time).\n- `--audit-names` prints the project-name audit (T050 and T071) as\n  `<project-path>: warning[T0xx]: <message>`; requires a loaded project.\n- When a project loads but **no** `parameters.m1cfg` (or no `.m1dbc`) is\n  discovered, a `note:` line on stderr announces that **T041** (calibration\n  coverage) / **T042** (dbc-signal-range) are skipped — so a degraded CI run is\n  visible rather than silently green, mirroring the project-less mode note.\n- `--select <CODES>` / `--ignore <CODES>` (comma-separated T-codes) filter the\n  reported diagnostics, applying the same `[diagnostics]` semantics as the unified\n  `m1-tools.toml` the editors read (the nearest `m1-tools.toml` `[diagnostics]`\n  `ignore`/`select` is honoured too; explicit flags override the file). A non-empty\n  `--select` runs only the listed codes; `--ignore` drops the listed codes.\n- `--ignore-symbol <CODE:PATH>` (comma-separated `CODE:Symbol.Path` entries)\n  suppresses a project-level code for exactly one symbol; the\n  `[diagnostics] ignore_symbols` list in `m1-tools.toml` is honoured the same\n  way (flags replace the file list).\n- `--rules` prints the T-code catalogue (with `--format json`, as JSON) and exits.\n- `--explain <CHANNEL>` prints one channel's solved project-wide invalid-value\n  (NaN/Inf) status and exits: the backward provenance chain when tainted (plus a\n  latch note), or a clean note otherwise. Accepts the canonical\n  `Root.`-qualified or the bare channel path; pass every project script as\n  FILES so the solve sees the whole graph. With `--format json`, emits\n  `{\"version\":1,\"explain\":{\"channel\":…,\"invalid\":…,\"latched\":…,\"chain\":[…]}}`.\n  A query, not a check: exits `0` either way, `2` for a missing project or an\n  unknown channel.\n- `--format json` emits one machine-parsable document\n  (`{\"version\":1,\"files\":[…],\"project\":[…],\"summary\":{…}}`) instead of the human\n  lines; syntax errors and project audits are included.\n- Opt-in rule **T064** (`wrong-argument-count`) — flags a call to a fully-modelled\n  library method whose argument count matches none of that method's overload\n  arities (union-aware; e.g. `Change.By` accepts 2 or 3). Off by default; enable\n  with `--select T064` (or `[diagnostics] select`). It never flags unknown members\n  (valid firmware absent from the intrinsics export stays opaque, never reported).\n- Output: `path:line:col: severity[T0xx]: message`. Syntax errors print first.\n- Exit codes: `0` no errors, `1` ≥1 error, `2` invocation error. Most v2 rules are\n  `Warning` severity; the exception is **T070** (`Error`), which — like a syntax\n  error — causes a non-zero exit.\n\n## `m1-cfg-export` — export the expected `.m1cfg` parameter list\n\nA second binary reuses the symbol model to generate the `.m1cfg` parameter list a\nproject expects ([C-Nucifora/m1-tools#10](https://github.com/C-Nucifora/m1-tools/issues/10)).\nUse it to seed a new calibration file, as a diff target for drift, or — with\n`--missing-only` — as a CI check for parameters absent from the committed `.m1cfg`.\n\n```\nm1-cfg-export [--output <file>] [--format cfg|json|csv]\n              [--baseline <existing.m1cfg>] [--missing-only] <Project.m1prj>\n```\n\n```sh\nm1-cfg-export Project.m1prj                                  # full skeleton (real .m1cfg XML)\nm1-cfg-export Project.m1prj --format csv -o params.csv       # every parameter as a row\nm1-cfg-export Project.m1prj --missing-only --baseline parameters.m1cfg\n```\n\n- Output is the real MoTeC `.m1cfg` XML (`<Configuration><Group><Parameter><Cell\n  Type=… Unit=…>`), with names *un*prefixed (`Foo.Bar`, not `Root.Foo.Bar`), or\n  `json`/`csv` (which also carry the `.m1prj` `security`, not part of a `.m1cfg`).\n- A parameter's cell `Type` is the exact storage type the `.m1prj` declares\n  (`f32`/`u32`/`s16`/…), `enum` for any enum-typed parameter (including\n  package-qualified types like `MoTeC Types.Mode Enumeration`), or the type a\n  `--baseline`/auto-discovered `.m1cfg` back-resolves. Parameters with no type in\n  either source (e.g. the `*.Value` of an untyped reference that isn't yet\n  calibrated) are emitted with a flagged `Type=\"\"` placeholder rather than a\n  guessed type.\n- `--baseline` is required by `--missing-only`; otherwise a sibling\n  `parameters.m1cfg` is auto-discovered (as the type checker does) purely to\n  enrich types.\n- Scope: exports `BuiltIn.Parameter` components. `BuiltIn.IOResourceConstant`\n  resource selections (which a real `.m1cfg` also lists) are not included yet.\n\n## Environment variables\n\n- `M1_PROJECT` — path to `Project.m1prj` (overrides discovery; used by the corpus\n  gate).\n- `M1_CONFIG` — path to `parameters.m1cfg` (overrides the sibling auto-discovery).\n- `M1_CORPUS_PATH` — directory of `.m1scr` scripts for the corpus gate\n  (`tests/corpus.rs`), which loads the project and checks every script without\n  panicking. The gate is skipped if neither the env vars nor the sibling example\n  project are present.\n\n## Note on examples\n\nExample identifiers in the docs and fixtures are **synthetic placeholders**, not\ndrawn from any real project.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "700d5ff",
        "date": "2026-06-11",
        "message": "fix: resolve enum type names case-insensitively, warn on case-variant heads (T020) (#184)"
      },
      {
        "hash": "f50f703",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog, install/verify docs, SHA256SUMS (#182)"
      },
      {
        "hash": "4df855f",
        "date": "2026-06-10",
        "message": "chore: bump version to 0.32.0 (#181)"
      },
      {
        "hash": "c33447f",
        "date": "2026-06-10",
        "message": "feat: builtin enum catalogue + full library reference from M1 Build help captures (#180)"
      },
      {
        "hash": "ad3dba9",
        "date": "2026-06-10",
        "message": "feat: T096 multiple-scheduled-writers (M1 Build Error 1022) (#179)"
      },
      {
        "hash": "5c81b95",
        "date": "2026-06-10",
        "message": "fix: promote T031 to Error severity for M1 Build 1338/1352 parity (#178)"
      },
      {
        "hash": "39ab618",
        "date": "2026-06-10",
        "message": "chore: bump version to 0.31.1 (#176)"
      },
      {
        "hash": "772ac4f",
        "date": "2026-06-10",
        "message": "fix: T021 Error severity + coverage for IO-method switch objects (#175)"
      }
    ]
  },
  {
    "slug": "m1-ci",
    "title": "m1-ci",
    "pitch": "Reusable GitHub Actions workflows that format-check, lint, and type-check MoTeC M1 script projects",
    "stack": [
      "Shell",
      "ci",
      "github-actions",
      "language-tooling",
      "m1-script"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-ci",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-ci\n\nReusable GitHub Actions workflows for [MoTeC M1](https://www.motec.com.au/) script\nprojects. Zero config — reference the workflow from your project and it\nformat-checks, lints, and type-checks every `.m1scr` with the\n[C-Nucifora M1 toolchain](https://github.com/C-Nucifora/m1-tools) on each push\nand pull request.\n\n## Usage\n\nAdd `.github/workflows/m1.yml` to your M1 project:\n\n```yaml\nname: M1\n\non:\n  push:\n  pull_request:\n\njobs:\n  check:\n    uses: C-Nucifora/m1-ci/.github/workflows/check.yml@v0.9.3\n    with:\n      scripts-path: UQR-EV/01.00/Scripts\n      project-file: UQR-EV/01.00/Project.m1prj\n```\n\nA ready-to-copy version lives in [`examples/check.yml`](examples/check.yml).\n\n## What it runs\n\nThe reusable [`check.yml`](.github/workflows/check.yml) installs the toolchain and\nruns it over every `.m1scr` it finds. Each check is its **own job**, so they run\nin parallel and report **independently** — a failing format check no longer skips\nthe lint and type check. You get a separate ✓/✗ status on the PR for each:\n\n| Check (job) | Tool | Fails when… |\n|-------------|------|-------------|\n| Format check (m1-fmt) | `m1-fmt --check` | a script is not canonically formatted |\n| Lint (m1-lint) | `m1-lint` | an **error**-severity lint fires (or a syntax error) |\n| Type check (m1-typecheck) | `m1-typecheck` | an **error**-severity type diagnostic fires |\n| Project validation (m1-project) | `m1-project validate` | an error-level structural finding in `Project.m1prj` (skipped silently when no project file exists) |\n\nBecause the jobs are independent, a single PR can show e.g. *Format check ✗ /\nLint ✓ / Type check ✗* at once — you see every problem in one run instead of\nfixing formatting just to discover the next failure.\n\nSet [`fail-on-warning`](#inputs) to also fail on warning-severity diagnostics\n(line length, complexity, `eq`-over-`==`, …), which otherwise only annotate.\n\nSet [`sarif-upload`](#inputs) to additionally push the lint findings to GitHub\n**code scanning** (Security tab, per-rule alert dismissal, new-alerts diffing on\nPRs) via `m1-lint --format sarif` + `github/codeql-action/upload-sarif`. The\ncalling job must grant `permissions: security-events: write`:\n\n```yaml\njobs:\n  check:\n    permissions:\n      contents: read\n      security-events: write\n    uses: C-Nucifora/m1-ci/.github/workflows/check.yml@v0.12.0\n    with:\n      sarif-upload: true\n```\n\nIf a `parameters.m1cfg` sits beside your `Project.m1prj`, the type checker\nauto-discovers it and uses its parameter **value types and units** — so the type\ncheck is parameter-type-aware with no extra configuration.\n\nDiagnostics are emitted as **inline annotations** on the pull request — each\n`m1-lint` / `m1-typecheck` finding lands on its exact line, and unformatted files\nare flagged by `m1-fmt`.\n\nScript names containing spaces (e.g. `CAN.Mission Critical Transcieve 500Hz.m1scr`)\nare handled correctly (NUL-delimited file lists).\n\n## Local checks (pre-commit)\n\nThe same gates run locally as [pre-commit](https://pre-commit.com) hooks, so a\ndeveloper's commit is checked with the **exact tools and versions CI uses**. Add\nto your project's `.pre-commit-config.yaml`, pinning the **same m1-ci tag** as your\nworkflow:\n\n```yaml\nrepos:\n  - repo: https://github.com/C-Nucifora/m1-ci\n    rev: v0.7.0          # same tag as `uses: …@v0.7.0` in your workflow\n    hooks:\n      - id: m1-fmt\n      - id: m1-lint\n      - id: m1-typecheck\n      - id: m1-project-validate\n```\n\nThen `pre-commit install`. Each hook downloads (once, cached under\n`~/.cache/m1-ci`) the pinned tool binary named in [`tools.env`](tools.env) — the\nsame version `check.yml` installs — so there is nothing to install by hand.\nPrebuilt binaries cover macOS (Apple Silicon), Windows, and Linux x86-64; other\nhosts build from source via `cargo` (the pinned tag).\n\nBecause both halves read `tools.env` (CI enforces that `check.yml`'s defaults\nmatch it), **one m1-ci tag pins one toolchain for both local and CI** — they can't\ndrift.\n\n## How the tools are installed\n\n`check.yml` downloads the **prebuilt release binaries** published by each tool\nrepo (`m1-fmt` / `m1-lint` / `m1-typecheck` / `m1-project` — the last lives\nunder `nedlane/`, the rest under `C-Nucifora/`) for the runner. If a release is\nunavailable for the requested version, it transparently falls back to building\nfrom source (`cargo install --git`).\n\nThe tool versions are **pinned by this m1-ci release** — the `fmt-version` /\n`lint-version` / `typecheck-version` defaults baked into `check.yml` name exact\ntags. So pinning `m1-ci@vX.Y.Z` installs a **frozen, reproducible toolchain**: a\nnew (possibly stricter) tool release can't change your CI result until you bump\nthe m1-ci tag. To track the newest tools instead, set `tools-version: latest` (or\noverride a single tool's `*-version`).\n\n## Inputs\n\n| Input | Default | Description |\n|-------|---------|-------------|\n| `scripts-path` | `.` | Directory searched recursively for `.m1scr` files. |\n| `project-file` | `\"\"` | `Project.m1prj` for symbol-aware type checking. Empty = `m1-typecheck` auto-discovers the nearest one upward from each script. |\n| `fmt-version` | `v0.8.2` | `m1-fmt` release to install: a tag, or `latest`. |\n| `lint-version` | `v0.12.2` | `m1-lint` release to install: a tag, or `latest`. |\n| `typecheck-version` | `v0.25.5` | `m1-typecheck` release to install: a tag, or `latest`. |\n| `tools-version` | `\"\"` | Master override (advanced). Empty = use the pinned per-tool versions above. `latest` = newest of every tool; a single tag forces all three (they are independently versioned). |\n| `run-fmt` | `true` | Run the formatter check. |\n| `run-lint` | `true` | Run the linter. |\n| `run-typecheck` | `true` | Run the type checker. |\n| `project-version` | `v0.3.1` | `m1-project` release to install: a tag, or `latest`. |\n| `run-project-validate` | `true` | Validate `Project.m1prj` structure with `m1-project validate` (explicit `project-file`, else auto-discovered; skips silently when absent). |\n| `sarif-upload` | `false` | Also emit `m1-lint` findings as SARIF and upload to GitHub **code scanning**. The calling job must grant `permissions: security-events: write`, and code scanning must be available on the repo (public, or Advanced Security). |\n| `fail-on-warning` | `false` | Fail on warning-severity diagnostics, not just errors. |\n\n## Pinning\n\n`@main` always tracks the latest workflow **and** the newest pinned tool defaults\non `main`. For reproducible CI, pin to a tag — the tag freezes both the workflow\nand the M1 toolchain versions it installs, e.g.\n`uses: C-Nucifora/m1-ci/.github/workflows/check.yml@v0.9.3`. Bump the tag\ndeliberately when you want the newer toolchain.\n\n## Releasing\n\nReleases are automated. Bump the version in [`VERSION`](VERSION) in a PR; when it\nmerges to `main`, [`.github/workflows/release.yml`](.github/workflows/release.yml)\ncuts the matching `vX.Y.Z` tag + GitHub Release (with generated notes) if one\ndoesn't already exist. The job is idempotent — re-running it for an existing\nversion is a no-op — and can also be triggered manually from the Actions tab.\n\nWhen you bump `VERSION`, also bump the `m1-ci-ref` input default in\n[`check.yml`](.github/workflows/check.yml) and the pinned tag in\n[`examples/check.yml`](examples/check.yml) to the matching `vX.Y.Z`. CI's\n`ci-ref-pin` job enforces this — it fails if either drifts from `v$(cat\nVERSION)` — so the ref m1-ci checks itself out at can never lag the release.\n\n## License\n\nGPL-3.0 — see [LICENSE](LICENSE).\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "821cb8d",
        "date": "2026-06-11",
        "message": "chore: bump m1-typecheck to v0.33.0; release v0.14.0 (#36)"
      },
      {
        "hash": "d04d900",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog + consume/install docs (#35)"
      },
      {
        "hash": "1b57b34",
        "date": "2026-06-10",
        "message": "chore: bump m1-typecheck to v0.32.0; release v0.13.0 (#34)"
      },
      {
        "hash": "80f9f0c",
        "date": "2026-06-10",
        "message": "chore: pin m1-typecheck v0.31.1, release v0.12.3 (#33)"
      },
      {
        "hash": "4419351",
        "date": "2026-06-10",
        "message": "chore: pin m1-typecheck v0.31.0, release v0.12.2 (#31)"
      },
      {
        "hash": "81a31be",
        "date": "2026-06-10",
        "message": "chore: pin m1-typecheck v0.30.1, release v0.12.1 (#30)"
      },
      {
        "hash": "441451d",
        "date": "2026-06-10",
        "message": "chore: bump m1-typecheck to v0.30.0, m1-project to v0.4.0 (#29)"
      },
      {
        "hash": "e98d507",
        "date": "2026-06-10",
        "message": "feat: Project.m1prj validation gate + opt-in SARIF upload to code scanning; pin m1-lint v0.14.0 / m1-typecheck v0.29.0 (#28)"
      }
    ]
  },
  {
    "slug": "m1-lint",
    "title": "m1-lint",
    "pitch": "Neovim linter plugin for M1 Script (via nvim-lint)",
    "stack": [
      "Rust",
      "language-tooling",
      "linter",
      "m1-script",
      "motec"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-lint",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-lint\n\nA linter for the MoTeC M1 script language (`.m1scr`). It runs CST-only style and\ncorrectness rules over a parsed script — it knows nothing about the project's\nsymbol model (that is `m1-typecheck`'s job). It is both a **library** (consumed by\n`m1-lsp` as a diagnostic source) and a **CLI**.\n\n## Workspace layout\n\nThe M1 toolchain lives in **six separate repositories**. They are not published\nto crates.io; instead each crate pins its upstreams as **versioned git-tag Cargo\ndependencies** (e.g. `m1-core = { git = \"…/m1-core.git\", tag = \"v0.3.1\" }`), so\nthis crate **does** build from a standalone clone — Cargo fetches its upstreams\nfrom their tagged releases. Checking the whole set out as siblings under one\nparent directory is handy for cross-repo work, but is not required to build:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root)\n├── m1-core/          # parse / CST / diagnostics\n├── m1-lint/          # this crate\n├── m1-fmt/           # formatter\n├── m1-typecheck/     # type checker\n└── m1-lsp/           # language server; depends on the four above\n```\n\n**`m1-lint` depends on `m1-core`** (a git-tag dep that transitively pulls in\n`tree-sitter-m1`), and is itself a git-tag dependency of `m1-lsp`. (The\n`m1-example` corpus project, used by the corpus test, is an optional sibling\ncheckout.)\n\nBecause every dependency is pinned by tag, the coupling **is** visible on\nGitHub — each `Cargo.toml` names its upstreams and their versions, and\nDependabot opens bump PRs as new upstream tags ship. Cutting a new upstream\nrelease and bumping `tag = \"vX.Y.Z\"` in each consumer is what propagates a\nchange across the stack.\n\n## Rules\n\n| Code | Severity | Rule |\n|------|----------|------|\n| L001 | Warning | line too long (configurable threshold) |\n| L002 | Warning | trailing whitespace |\n| L003 | Warning | missing final newline |\n| L004 | Warning | prefer `eq` over `==` |\n| L005 | Warning | prefer the spelled logical operators (`and`/`or`/`not`) |\n| L006 | Error   | float compared with an equality operator |\n| L007 | Warning | missing space around an operator |\n| L008 | Warning | nesting too deep (configurable threshold) |\n| L009 | Warning | cyclomatic complexity too high — total decision points (configurable, default 40) |\n| L010 | Warning | indentation uses the wrong char (default: tabs required) |\n| L011 | Warning | comment-style violation |\n| L012 | Warning | local binding is never used |\n| L014 | Warning | `expand` variable `$(VAR)` not bound by an enclosing `expand` |\n| L015 | Warning | `local` declaration has no initializer (M1 requires one) |\n| L016 | Warning | `local` name not lowercase-initial / contains spaces |\n| L017 | Warning | magic number (unnamed numeric literal in an expression) — **opt-in**, enable with `--select L017` |\n| L018 | Warning | space in front of a `;` (manual: \"Do not put spaces in front of semicolons\") — fixable |\n| L019 | Warning | cognitive complexity too high — Sonar-style, nesting-weighted (configurable, default 15) |\n| L020 | Warning | object name begins lowercase (manual p.64: objects begin with an uppercase letter; locals are L016's side) |\n| L021 | Warning | more than one statement on a line (manual p.65, the first layout rule) |\n| L022 | Warning | no space between a keyword and `(` (`if(a)`) — fixable |\n| L023 | Warning | space between a function name and `(` (`Func (a)`) — fixable |\n| L024 | Warning | ternary condition not in parentheses (manual p.67: `(condition) ? a : b`) — fixable |\n| L025 | Warning | `local` only used inside one nested block deeper than its declaration (manual p.67: most constrained scope); static locals, call initializers and expand bodies exempt |\n\n`L009` (cyclomatic) counts every decision point equally and so grows with sheer\nsize; `L019` (cognitive) is nesting-weighted, so deeply-nested logic costs far\nmore than a long flat sequence. They are complementary: L019 is the primary\nreadability gate (`--max-cognitive-complexity`), L009 is a loose backstop for\npathological branch counts (`--max-complexity`).\n\nThe catalogue is also available machine-readably with `m1-lint --rules --format\njson` (schema `{\"version\":1,\"rules\":[{\"code\",\"name\",\"fixable\"}]}`), sourced\ndirectly from the `LintCode` enum so external tools can enumerate the rules\nwithout copying the list.\n\n### Suppressing a diagnostic — `// @m1:allow(...)`\n\nA `// @m1:allow(L0xx, …)` annotation (the toolchain-wide annotation framework,\nm1-core#33) suppresses the listed rule(s) on the construct it is attached to —\nthe M1 analogue of `// eslint-disable-next-line`. A bare `// @m1:allow`\nsuppresses every rule on that construct.\n\n```m1\n// @m1:allow(L010)\n    Indented With Spaces = 1;     // L010 not reported on this line\n\nFoo = some_long_expression; // @m1:allow(L008)   ← trailing form, attaches to this statement\n```\n\nThe annotation attaches **leading** (the next statement, so it stacks on\nconsecutive lines above its target) or **trailing** (a statement it follows on\nthe same line). Suppression is line-scoped to the target construct. (Reporting\nonly — `--fix` still applies mechanical fixes; most suppressible rules are\nnon-fixable.)\n\n### Per-rule severity overrides\n\n`.m1lint.toml` accepts a `[severity]` table mapping codes to\n`error|warning|info|hint`, applied after the rule runs — promote `L001` to a\nhard error in CI, or soften `L006`, without forking the rule set:\n\n```toml\n[severity]\nL001 = \"error\"\n```\n\n### Baselines\n\n`--write-baseline FILE` snapshots every current finding (anchored on the\noffending line's content, not its number, so unrelated edits don't resurrect\nsuppressed findings); later runs with `--baseline FILE` report only new\nregressions. The adoption path for a legacy codebase.\n\n## CLI usage\n\n```sh\nm1-lint <file.m1scr>...              # human-readable diagnostics\nm1-lint --format json <file.m1scr>   # machine-readable JSON\nm1-lint --fix <file.m1scr>           # apply safe autofixes in place\nm1-lint --rules                      # list every rule (add --format json)\nm1-lint --explain L022               # one rule's full rationale and fix behaviour\nm1-lint --fix --diff <file.m1scr>    # preview --fix as a unified diff (writes nothing)\nm1-lint --format sarif <files>       # SARIF 2.1.0 for GitHub code scanning etc.\nm1-lint --write-baseline .m1lint-baseline.json <files>   # snapshot current findings\nm1-lint --baseline .m1lint-baseline.json <files>         # report only NEW findings\n```\n\nFlags accept both the space-separated (`--format json`) and the GNU\n`--flag=value` (`--format=json`, `--select=L010`) forms.\n\nAutofixes are only applied when the fixed source re-parses and preserves the\nscript's semantic tokens. Rule selection and thresholds can be configured via a\n`.m1lint.toml` discovered upward from the input file (or passed explicitly), with\n`select`/`ignore` lists and per-rule thresholds. `indent-style` chooses the\nindentation L010 enforces — `\"tab\"` (default, per the M1 manual) or `\"spaces\"`.\nKeys may be written kebab-case (`max-line-length`) or snake_case\n(`max_line_length`), so the unified `m1-tools.toml` / `m1-lsp --scaffold-config`\noutput can be used directly as a `.m1lint.toml`.\n\n## Build & test\n\n```sh\ncargo build --release      # binary at target/release/m1-lint\ncargo test                 # unit + corpus + fixture + autofix-acceptance tests\n```\n\nThe corpus test runs every `.m1scr` under `$M1_CORPUS_PATH` (falling back to the\nsibling `m1-example` example project) and asserts the linter never panics; it is\nskipped if the directory is absent.\n\n## Note on examples\n\nExample identifiers in the docs and fixtures are **synthetic placeholders**, not\ndrawn from any real project.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "4b892b2",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog, install/verify docs, SHA256SUMS (#117)"
      },
      {
        "hash": "5b62307",
        "date": "2026-06-10",
        "message": "feat: L024 ternary-condition-parens (with autofix) + L025 local-scope-too-wide (#116)"
      },
      {
        "hash": "8395dcf",
        "date": "2026-06-09",
        "message": "feat: L020-L023 manual layout/naming rules + explain/SARIF/severity/baseline/diff (#113)"
      },
      {
        "hash": "0f4c3a9",
        "date": "2026-06-08",
        "message": "chore: align on m1-core v0.7.1 + m1-workspace v0.5.1 (#103)"
      },
      {
        "hash": "3b5726a",
        "date": "2026-06-08",
        "message": "fix(L006): don't treat hex literals as floats (false positive) (#100)"
      },
      {
        "hash": "c84a9c1",
        "date": "2026-06-08",
        "message": "fix: iterative lint walk (no stack overflow), linear L008, L007 covers local decls (#99)"
      },
      {
        "hash": "33a23fa",
        "date": "2026-06-08",
        "message": "refactor: single-source rule registry to cut per-rule edits (#98)"
      },
      {
        "hash": "b1b4bf9",
        "date": "2026-06-07",
        "message": "refactor: dedup into core/workspace, clap CLI, check_file_cst hook (#97)"
      }
    ]
  },
  {
    "slug": "m1-fmt",
    "title": "m1-fmt",
    "pitch": "Neovim formatter plugin for M1 Script (via conform.nvim)",
    "stack": [
      "Rust",
      "conform-nvim",
      "formatter",
      "language-tooling",
      "m1-script"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-fmt",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-11",
    "readme": "# m1-fmt\n\nAn auto-formatter (pretty-printer) for the MoTeC M1 script language (`.m1scr`).\nIt reparses the formatted output to guarantee it never changes a script's\nmeaning. It is both a **library** (consumed by `m1-lsp` for\n`textDocument/formatting`) and a **CLI**.\n\n## Workspace layout\n\nThe M1 toolchain lives in **six separate repositories**. They are not published\nto crates.io; instead each crate pins its upstreams as **versioned git-tag Cargo\ndependencies** (e.g. `m1-core = { git = \"…/m1-core.git\", tag = \"v0.3.1\" }`), so\nthis crate **does** build from a standalone clone — Cargo fetches its upstreams\nfrom their tagged releases. Checking the whole set out as siblings under one\nparent directory is handy for cross-repo work, but is not required to build:\n\n```\n<parent>/\n├── tree-sitter-m1/   # grammar (root)\n├── m1-core/          # parse / CST / diagnostics\n├── m1-lint/          # linter\n├── m1-fmt/           # this crate\n├── m1-typecheck/     # type checker\n└── m1-lsp/           # language server; depends on the four above\n```\n\n**`m1-fmt` depends on `m1-core`** (a git-tag dep that transitively pulls in\n`tree-sitter-m1`), and is itself a git-tag dependency of `m1-lsp`. (The\n`m1-example` corpus project, used by the corpus test, is an optional sibling\ncheckout.)\n\nBecause every dependency is pinned by tag, the coupling **is** visible on\nGitHub — each `Cargo.toml` names its upstreams and their versions, and\nDependabot opens bump PRs as new upstream tags ship. Cutting a new upstream\nrelease and bumping `tag = \"vX.Y.Z\"` in each consumer is what propagates a\nchange across the stack.\n\n## Guarantees (invariants)\n\nEvery format is verified to uphold three invariants — also exercised by a\nproperty-based fuzz suite:\n\n1. **Idempotency** — formatting already-formatted output is a no-op.\n2. **Output reparses** — the result parses without new syntax errors.\n3. **Semantic-token preservation** — the sequence of meaningful tokens is\n   unchanged; only trivia (whitespace/layout) moves.\n\nIf the input has syntax errors, `m1-fmt` returns it unchanged (pass-through\nsafety).\n\n## Formatting behavior\n\n- **Manual-correct style by default:** Allman braces (each brace on its own line)\n  and tab indentation, per the M1 Build Development Manual. Override in\n  `.m1fmt.toml` for a different house style:\n  ```toml\n  brace_style  = \"allman\"   # or \"kr\"\n  indent_style = \"tab\"      # or \"spaces\"\n  indent_width = 4          # columns per level (tab display width / space count)\n  ```\n- Consistent indentation, operator spacing, and brace/statement layout.\n- **Line-wrapping** at the width budget: argument lists (greedy fill, no trailing\n  comma), binary chains (break before operators), and `if` conditions, accounting\n  for trailing end-of-line comments, `;`, and `,`.\n- **`--max-blank-lines <n>`** collapses runs of blank lines and strips\n  brace-adjacent blanks; author blank lines are otherwise preserved.\n- **Blank line after `when` blocks** (manual p.65: \"all functions and methods to\n  end with a blank line\"): one blank is inserted after a top-level `when` block\n  when missing (never inside nested blocks, never at EOF).\n- **Opt-in `.m1fmt.toml` extras** (off by default — neither is a manual\n  mandate):\n  ```toml\n  align_assignments = true   # align `=` in runs of simple assignments\n  reflow_comments   = true   # split over-width `//` comments (never joins\n                             # short lines; never touches @m1: annotations)\n  ```\n- **Format-off regions** (`@m1:fmt`): the lines from a standalone\n  `// @m1:fmt(off)` comment through the next standalone `// @m1:fmt(on)`\n  (markers inclusive) pass through the formatter byte-for-byte — for\n  hand-aligned constant tables and other deliberate layout. An unclosed `off`\n  runs to end of file; a marker sharing a line with code is inert; `--range`\n  requests touching an off region are declined. Off-region lines are exempt\n  from line-too-long warnings.\n- **Range formatting** (`--range`, LSP `rangeFormatting`) snaps to the deepest\n  run of complete statements covering the request — formatting one line inside\n  a long `when` block reformats just that statement, re-indented in place.\n\n## CLI usage\n\n```sh\nm1-fmt <file.m1scr>                  # print formatted output to stdout\nm1-fmt --max-blank-lines 1 <file>    # cap consecutive blank lines\nm1-fmt --range 10:14 <file>          # format only lines 10–14, leave the rest as-is\n```\n\n`--range START:END` (1-based, inclusive) formats only the top-level statements\noverlapping that line range and leaves every other line byte-for-byte unchanged;\nthe range is snapped outward to whole statement boundaries. It composes with\n`--check`, `--in-place`, and `--diff`, and backs editor format-on-selection (the\nLSP calls the underlying `format_range` to build `textDocument/rangeFormatting`\nedits). Expression fragments aren't independently parseable, so a range that\noverlaps no complete statement is a no-op.\n\nA buffer with syntax errors is left byte-for-byte unchanged (formatting never\ncorrupts unparseable input). In `--check` mode this is reported on stderr and\nexits non-zero, so CI doesn't mistake an unparseable file for a clean one.\n\n## Build & test\n\n```sh\ncargo build --release      # binary at target/release/m1-fmt\ncargo test                 # unit + snapshot + corpus + proptest-invariant tests\n```\n\nThe corpus test formats every `.m1scr` under `$M1_CORPUS_PATH` (falling back to\nthe sibling `m1-example` example project), checking the invariants and that no\nbreakable line exceeds the width budget; it is skipped if the directory is absent.\n\n## Note on examples\n\nExample identifiers in the docs and fixtures are **synthetic placeholders**, not\ndrawn from any real project.\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n\n## Trademark\n\nIndependent, community-built open-source tooling for the MoTeC® M1 script\nlanguage. Not affiliated with, authorised, or endorsed by MoTeC Pty Ltd.\n\"MoTeC\" and \"M1\" are trademarks of MoTeC Pty Ltd.\n",
    "commits": [
      {
        "hash": "a3f5950",
        "date": "2026-06-11",
        "message": "Format-off regions: honour @m1:fmt(off) / @m1:fmt(on) markers (#103)"
      },
      {
        "hash": "d4aa7bd",
        "date": "2026-06-11",
        "message": "ci: richer releases — generated changelog, install/verify docs, SHA256SUMS (#101)"
      },
      {
        "hash": "5629741",
        "date": "2026-06-09",
        "message": "feat: blank-after-when, align_assignments, reflow_comments, deep range formatting (#95-#98) (#100)"
      },
      {
        "hash": "6357711",
        "date": "2026-06-08",
        "message": "chore: align on m1-core v0.7.1 + m1-workspace v0.5.1 (#92)"
      },
      {
        "hash": "c9333c7",
        "date": "2026-06-08",
        "message": "Keep a comment between `when (subject)` and its `{` before the brace (#91)"
      },
      {
        "hash": "195f88f",
        "date": "2026-06-08",
        "message": "fix: EOL comment attachment, preserve BOM, graceful broken-pipe (#90)"
      },
      {
        "hash": "cdb0d27",
        "date": "2026-06-08",
        "message": "refactor: document why printer keeps inline operator matches over shared predicates (#89)"
      },
      {
        "hash": "1a8dad6",
        "date": "2026-06-07",
        "message": "refactor: split printer, dedup into core/workspace, drop dead code (#88)"
      }
    ]
  },
  {
    "slug": "m1-tools",
    "title": "m1-tools",
    "pitch": "Developer tools for the MoTeC M1 scripting language",
    "stack": [
      "developer-tools",
      "language-tooling",
      "m1-script",
      "motec"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/m1-tools",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-07",
    "readme": "# M1 Toolchain\n\nA suite of developer tools for the MoTeC M1 scripting language (`.m1scr`), providing syntax highlighting, language server support, formatting, and linting across all major editors.\n\n## Getting Started\n\nInstall [vcstool](https://github.com/dirk-thomas/vcstool):\n\n```sh\npip install vcstool\n```\n\nClone this repo and all sub-repos:\n\n```sh\ngit clone https://github.com/C-Nucifora/m1-tools.git\ncd m1-tools\nvcs import .. < m1-tools.repos\n```\n\nEach sub-repo is cloned as a sibling of `m1-tools`. To update all sub-repos to latest `main`:\n\n```sh\nvcs pull .. < m1-tools.repos\n```\n\n## Tools\n\n| Repo | Purpose |\n| --- | --- |\n| [tree-sitter-m1](https://github.com/C-Nucifora/tree-sitter-m1) | Tree-sitter grammar + Rust bindings |\n| [m1-core](https://github.com/C-Nucifora/m1-core) | CST helpers and diagnostics library |\n| [m1-workspace](https://github.com/nedlane/m1-workspace) | Shared filesystem/path conventions library that the tools depend on |\n| [m1-lint](https://github.com/C-Nucifora/m1-lint) | Static analysis / linter (12 rules) |\n| [m1-lsp](https://github.com/C-Nucifora/m1-lsp) | Language Server Protocol implementation |\n| [m1-vscode](https://github.com/nedlane/m1-vscode) | VS Code extension |\n| [m1-typecheck](https://github.com/C-Nucifora/m1-typecheck) | Type/symbol model and type-rule diagnostics |\n| [m1-fmt](https://github.com/C-Nucifora/m1-fmt) | Code formatter |\n| [m1-project](https://github.com/nedlane/m1-project) | Validated CLI editor for `Project.m1prj` (create channels, set permissions/unit/type, set call rate) — invoked by the editors |\n| [nvim-m1](https://github.com/C-Nucifora/nvim-m1) | Neovim plugin (LSP + tree-sitter + lint + fmt) |\n| [telescope-m1.nvim](https://github.com/C-Nucifora/telescope-m1.nvim) | Telescope extension: symbol picker, component browser |\n| [m1-ci](https://github.com/C-Nucifora/m1-ci) | Reusable GitHub Actions workflows for M1 projects |\n\n### tree-sitter-m1\n\nA [Tree-sitter](https://tree-sitter.github.io/tree-sitter/) grammar for M1 script. Provides:\n\n- Incremental, error-recovering parsing\n- Syntax highlighting queries (`.scm`)\n- Rust bindings via `m1_tree_sitter`\n- Corpus gate: all 80 EV-M1 production scripts parse with 0 errors (`scripts/check-corpus.sh`)\n\n### m1-core\n\nThe shared parsing and diagnostics library used by all other tools. Exposes:\n\n```rust\nuse m1_core::{parse, Cst, Node, Kind, Diagnostic, Code, Severity, Range, Position};\n```\n\nAll higher-level tools (`m1-typecheck`, `m1-lsp`, `m1-fmt`, `m1-lint`) depend on this crate.\nTree-sitter is fully confined to this crate — consumers never see it directly.\n\n### m1-lint\n\nA static analysis tool that enforces M1 style and correctness rules. Twelve rules ship in v1:\n\n| Code | Rule |\n| --- | --- |\n| L001 | line-too-long (configurable, default 88) |\n| L002 | trailing-whitespace |\n| L003 | missing-final-newline |\n| L004 | eq-operator-preferred (`==` → `eq`, `!=` → `neq`) |\n| L005 | logical-operator-preferred (`&&` → `and`, `\\|\\|` → `or`, `!` → `not`) |\n| L006 | float-eq-comparison (heuristic; suppressed when type model is loaded) |\n| L007 | operator-spacing |\n| L008 | nesting-too-deep (configurable, default 4) |\n| L009 | cyclomatic-complexity (configurable, default 10) |\n| L010 | tab-for-indentation |\n| L011 | comment-style |\n| L012 | unused-local |\n\nConfigure via `.m1lint.toml` in the project root. Rules can be individually enabled, disabled, and tuned.\n\n### m1-lsp\n\nA Language Server Protocol server for M1 script. Integrates `m1-core`, `m1-typecheck`, and `m1-lint` behind a single LSP binary. Capabilities:\n\n| Category | Features |\n| --- | --- |\n| Diagnostics | Syntax errors, 12 lint rules, type diagnostics (when project loaded) |\n| Navigation | Go-to-definition, find-all-references, document highlight, workspace symbol search |\n| Editing | Completion (trigger: `.`), signature help, rename with prepare-rename, code actions |\n| Display | Hover, inlay type-hints, semantic tokens (full), folding ranges, document outline |\n| Formatting | `textDocument/formatting` + range formatting (stub — active once `m1-fmt` ships) |\n| Project | Discovers and loads `Project.m1prj` + `.m1cfg` + `.m1dbc`; watches for changes |\n\n> **Note:** Formatting returns no edits until `m1-fmt` is implemented. All other features are active.\n\n### m1-vscode\n\nThe VS Code extension for M1 script. Bundles the `m1-lsp` binary for macOS (Apple Silicon), Linux (x86-64), and Windows (x86-64). Install from the [Releases page](https://github.com/nedlane/m1-vscode/releases):\n\n```sh\ncode --install-extension m1-vscode-<platform>.vsix\n```\n\nFeatures: syntax highlighting, diagnostics, hover, completion, go-to-definition, find references, rename, inlay hints, semantic tokens, code actions, formatting, folding, and document/workspace symbol search.\n\n### m1-typecheck\n\nThe type and symbol model for M1 script. Loads `Project.m1prj` symbol tables (channels, parameters, enums, DBC signals) and exposes them to `m1-lsp` for hover, completion, rename, and inlay hints.\n\n### m1-fmt\n\nAn opinionated code formatter for `.m1scr` files, modelled on `rustfmt`.\n\n---\n\n## Command-line tools\n\n`m1-fmt`, `m1-lint`, and `m1-typecheck` run standalone on `.m1scr` scripts\n(no editor required) — ideal for CI. See **[docs/cli.md](docs/cli.md)** for a\nper-tool quickstart and the shared conventions (exit codes, `--version`,\n`--help`, and the layered `m1-tools.toml` / `.m1fmt.toml` / `.m1lint.toml`\nconfiguration).\n\n---\n\n## Editor Setup\n\n### VS Code\n\nInstall the [m1-vscode](https://github.com/nedlane/m1-vscode) extension — it bundles everything. No separate installs required.\n\n### Neovim\n\nPrerequisites: [nvim-treesitter](https://github.com/nvim-treesitter/nvim-treesitter), [nvim-lspconfig](https://github.com/neovim/nvim-lspconfig). Optionally [conform.nvim](https://github.com/stevearc/conform.nvim) and [nvim-lint](https://github.com/mfussenegger/nvim-lint).\n\nAdd to your [lazy.nvim](https://github.com/folke/lazy.nvim) plugin spec (e.g. `~/.config/nvim/lua/plugins/m1.lua`):\n\n```lua\nreturn {\n  -- 1. Tree-sitter grammar: syntax highlighting and indentation\n  {\n    \"nvim-treesitter/nvim-treesitter\",\n    opts = function(_, opts)\n      -- Register the m1 grammar before nvim-treesitter setup\n      local parser_config = require(\"nvim-treesitter.parsers\").get_parser_configs()\n      parser_config.m1 = {\n        install_info = {\n          url   = \"https://github.com/C-Nucifora/tree-sitter-m1\",\n          files = { \"src/parser.c\", \"src/scanner.c\" },\n          branch = \"main\",\n        },\n        filetype = \"m1scr\",\n      }\n      opts.ensure_installed = opts.ensure_installed or {}\n      vim.list_extend(opts.ensure_installed, { \"m1\" })\n    end,\n  },\n\n  -- 2. LSP: diagnostics, hover, completion, rename, go-to-definition, inlay hints\n  {\n    \"neovim/nvim-lspconfig\",\n    config = function()\n      local lspconfig = require(\"lspconfig\")\n      local configs   = require(\"lspconfig.configs\")\n\n      -- Register m1-lsp (not yet upstream in nvim-lspconfig)\n      if not configs.m1_lsp then\n        configs.m1_lsp = {\n          default_config = {\n            cmd              = { \"m1-lsp\" },   -- must be on $PATH, or use full path\n            filetypes        = { \"m1scr\" },\n            root_dir         = lspconfig.util.root_pattern(\"Project.m1prj\", \".git\"),\n            single_file_support = true,\n          },\n        }\n      end\n      lspconfig.m1_lsp.setup({})\n    end,\n  },\n\n  -- 3. Format-on-save via conform.nvim (no-op until m1-fmt ships)\n  {\n    \"stevearc/conform.nvim\",\n    opts = {\n      formatters_by_ft = { m1scr = { \"m1_fmt\" } },\n      formatters = {\n        m1_fmt = {\n          command = \"m1-fmt\",\n          args    = { \"--stdin-filepath\", \"$FILENAME\" },\n          stdin   = true,\n        },\n      },\n    },\n  },\n\n  -- 4. Standalone lint via nvim-lint (supplements LSP diagnostics)\n  {\n    \"mfussenegger/nvim-lint\",\n    config = function()\n      local lint = require(\"lint\")\n      lint.linters.m1_lint = {\n        cmd            = \"m1-lint\",\n        stdin          = true,\n        args           = { \"--stdin-filepath\", function() return vim.api.nvim_buf_get_name(0) end },\n        stream         = \"stdout\",\n        ignore_exitcode = true,\n        parser         = require(\"lint.parser\").from_pattern(\n          \"([^:]+):(%d+):(%d+): (%a+): %[([LT]%d+)%] (.+)\",\n          { \"file\", \"lnum\", \"col\", \"severity\", \"code\", \"message\" }\n        ),\n      }\n      lint.linters_by_ft = { m1scr = { \"m1_lint\" } }\n      vim.api.nvim_create_autocmd({ \"BufWritePost\", \"BufReadPost\" }, {\n        callback = function() lint.try_lint() end,\n      })\n    end,\n  },\n}\n```\n\nAdd `.m1scr` file type detection (e.g. in `~/.config/nvim/lua/vim-options.lua`):\n\n```lua\nvim.filetype.add({ extension = { m1scr = \"m1scr\" } })\n```\n\n### Zed\n\nAdd to `settings.json`:\n\n```json\n{\n  \"lsp\": {\n    \"m1-lsp\": {\n      \"binary\": { \"path\": \"/path/to/m1-lsp/target/release/m1-lsp\" }\n    }\n  },\n  \"languages\": {\n    \"M1 Script\": { \"language_servers\": [\"m1-lsp\"] }\n  }\n}\n```\n\nTree-sitter grammar support in Zed requires the grammar to be registered; follow the [Zed extension guide](https://zed.dev/docs/extensions/languages) using `tree-sitter-m1` as the grammar source.\n\n### Helix\n\nAdd to `~/.config/helix/languages.toml`:\n\n```toml\n[[language]]\nname             = \"m1scr\"\nscope            = \"source.m1scr\"\nfile-types       = [\"m1scr\"]\nroots            = [\"Project.m1prj\"]\nlanguage-servers = [\"m1-lsp\"]\nformatter        = { command = \"m1-fmt\", args = [\"--stdin-filepath\", \"%\"] }\n\n[language-server.m1-lsp]\ncommand = \"/path/to/m1-lsp/target/release/m1-lsp\"\n```\n\nPlace the Tree-sitter grammar under `~/.config/helix/runtime/grammars/` following Helix's [adding languages guide](https://docs.helix-editor.com/guides/adding_languages.html).\n\n### Emacs (eglot)\n\n```elisp\n(add-to-list 'auto-mode-alist '(\"\\\\.m1scr\\\\'\" . prog-mode))\n(with-eval-after-load 'eglot\n  (add-to-list 'eglot-server-programs\n               '(prog-mode . (\"/path/to/m1-lsp/target/release/m1-lsp\"))))\n(add-hook 'prog-mode-hook\n  (lambda ()\n    (when (string= (file-name-extension (or buffer-file-name \"\")) \"m1scr\")\n      (eglot-ensure))))\n```\n\n---\n\n## Building from Source\n\nAll Rust tools require stable Rust via [rustup](https://rustup.rs/).\n\n```sh\ngit clone https://github.com/C-Nucifora/m1-lsp\ncd m1-lsp\ncargo build --release\n# Binary: ./target/release/m1-lsp\n```\n\n`tree-sitter-m1` additionally requires Node.js and the Tree-sitter CLI:\n\n```sh\nnpm install -g tree-sitter-cli\ncd tree-sitter-m1\ntree-sitter generate\ncargo build --release\n```\n\n### nvim-m1\n\nA [lazy.nvim](https://github.com/folke/lazy.nvim) plugin that wires tree-sitter, nvim-lspconfig, conform.nvim, and nvim-lint together in a single `require(\"nvim-m1\").setup({})` call — the Neovim equivalent of m1-vscode.\n\nInstall in your plugin spec:\n\n```lua\n{\n  \"C-Nucifora/nvim-m1\",\n  dependencies = {\n    \"nvim-treesitter/nvim-treesitter\",\n    \"neovim/nvim-lspconfig\",\n    \"stevearc/conform.nvim\",\n    \"mfussenegger/nvim-lint\",\n  },\n  opts = {},\n}\n```\n\n### telescope-m1.nvim\n\nA [Telescope](https://github.com/nvim-telescope/telescope.nvim) extension providing:\n\n- **Workspace symbols** — fuzzy-search channels, parameters, and enums in the loaded project\n- **Component browser** — browse the `.m1prj` component hierarchy\n- **Lint rule picker** — toggle or navigate `m1-lint` rules\n\nInstall alongside `nvim-m1`:\n\n```lua\n{\n  \"C-Nucifora/telescope-m1.nvim\",\n  dependencies = { \"nvim-telescope/telescope.nvim\", \"C-Nucifora/nvim-m1\" },\n  opts = {},\n}\n```\n\n### m1-ci\n\nReusable GitHub Actions workflows for any M1 script project. Reference the shared workflow:\n\n```yaml\n# .github/workflows/check.yml\njobs:\n  m1-check:\n    uses: C-Nucifora/m1-ci/.github/workflows/check.yml@main\n```\n\nRuns: `m1-lint` on all `.m1scr` files, `m1-typecheck` project validation, and a corpus parse gate.\n\n> **Tip:** pin to a release tag once published (for example `@v0.1.0`) to avoid workflow drift.\n\n---\n\n## Architecture\n\n```text\ntree-sitter-m1        ← grammar (C + Rust bindings)\n      ↑\n  m1-core            ← CST helpers + shared diagnostics\n      ↑\n  ┌───┴──────────┬──────────┐\nm1-typecheck   m1-fmt    m1-lint    ← domain libraries\n      ↑                    ↑\n      └──────────┬──────────┘\n             m1-lsp         ← LSP server (integrates all)\n                 ↑\n           m1-vscode        ← VS Code extension\n```\n\n`m1-core` and `m1-typecheck` are library crates. `m1-fmt` and `m1-lint` expose both a CLI and a library API. `m1-lsp` integrates all three behind a single LSP binary.\n\n---\n\n## License\n\nLicensed under the GNU General Public License v3.0 or later (GPL-3.0-or-later) — see [LICENSE](LICENSE).\n\nCopyright (C) 2026 The M1 Tools authors.\n",
    "commits": [
      {
        "hash": "86f7c59",
        "date": "2026-06-07",
        "message": "Add CLI quickstart + conventions docs (#17, #16) (#18)"
      },
      {
        "hash": "34066c1",
        "date": "2026-06-03",
        "message": "Add nedlane/m1-project to the toolchain (#14)"
      },
      {
        "hash": "96fa35b",
        "date": "2026-06-02",
        "message": "Merge pull request #13 from C-Nucifora/copilot/update-readme"
      },
      {
        "hash": "2923835",
        "date": "2026-06-02",
        "message": "docs: remove stale status/version claims from README"
      },
      {
        "hash": "6ee9997",
        "date": "2026-06-02",
        "message": "Initial plan"
      },
      {
        "hash": "d9a134a",
        "date": "2026-06-02",
        "message": "Add nedlane/m1-workspace to the toolchain manifest (#11)"
      },
      {
        "hash": "04dae12",
        "date": "2026-06-02",
        "message": "Merge pull request #9 from C-Nucifora/docs/add-new-repos"
      },
      {
        "hash": "e88fe10",
        "date": "2026-06-02",
        "message": "docs: improve README details for new repos"
      }
    ]
  },
  {
    "slug": "iOS-Shortcuts",
    "title": "iOS-Shortcuts",
    "pitch": "Code project — see the README.",
    "stack": [],
    "sourceUrl": "https://github.com/C-Nucifora/iOS-Shortcuts",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-06-01",
    "readme": "# iOS Shortcuts\n\nVersion-controlled backup of my Shortcuts, edited via the `shortcuts-playground` Claude Code plugin.\n\n## Layout\n- `shortcuts/` — canonical unsigned `.xml` plists (source of truth, diff-friendly)\n- `signed/` — built `.shortcut` files for one-tap install (rebuild on demand)\n\n## Workflow\n- **Create**: `/shortcuts-playground:build <brief>` → save XML to `shortcuts/<name>.xml` → commit\n- **Edit**: `/shortcuts-playground:remix shortcuts/<name>.xml <change>` → commit diff\n- **Install on device**: build to `signed/<name>.shortcut`, then AirDrop or open the GitHub raw URL on iOS\n\n## Importing from the Shortcuts app\n### macOS (easiest)\n1. Open Shortcuts.app\n2. Right-click the shortcut → **Copy** (or File → Export → unsigned)\n3. Save the `.shortcut` file somewhere\n4. Hand the path to Claude; the playground skill converts it to XML in `shortcuts/`\n\n### iOS\n1. Open the shortcut → share sheet → **Share Shortcut** → *File*\n2. AirDrop or save to iCloud Drive → pull onto your Mac → same as above\n\n> Signed `.shortcut` files are AEA1-encrypted. The playground skill unwraps them to plist XML for git.\n\n## Log Meal to Health → Apple Health\n\nLogs a meal's nutrition into Apple Health from a payload Claude produces after\nanalyzing a meal photo. Files: `shortcuts/Log Meal to Health.xml` (source),\n`signed/Log Meal to Health.shortcut` (installable).\n\n**How to use with Claude (iOS):**\n1. Send Claude a photo of your meal.\n2. Ask it to \"estimate the nutrition and give me a Log Meal to Health payload\".\n3. Tap the run link it returns, or copy the payload text and run the shortcut.\n\nThe shortcut shows the values in an editable box and a date picker (defaulting\nto now) before writing anything, so you can correct an estimate or backdate a\nmeal. Any nutrient left blank is skipped. First run prompts for Health access.\n\n**Tell Claude to emit this exact format** — one `key: value` per line, and to\n**only include a nutrient when it is genuinely confident; omit, never guess**\n(a missing value is skipped cleanly, a fabricated one pollutes Health data):\n\n```\nname: <meal name>\ndate: now\ncalories: <kcal>\nprotein: <g>\ncarbs: <g>\ntotal_fat: <g>\n... (any of the supported keys below)\n```\n\n`name` and `date` are optional (`date` accepts `now` or a date string).\n\n**Run-link form** (one tap, payload pre-filled):\n\n```\nshortcuts://run-shortcut?name=Log%20Meal%20to%20Health&input=text=<URL-encoded payload>\n```\n\nIf no input is passed, the shortcut reads the payload from the clipboard\ninstead — so the copy/paste flow works with the same shortcut.\n\n**Supported keys** (unit in parentheses):\n`calories`(kcal), `carbs`(g), `protein`(g), `total_fat`(g), `saturated_fat`(g),\n`monounsaturated_fat`(g), `polyunsaturated_fat`(g), `cholesterol`(mg),\n`sugar`(g), `fiber`(g), `sodium`(mg), `water`(mL), `caffeine`(mg),\n`vitamin_a`(mcg), `thiamin`(mg), `riboflavin`(mg), `niacin`(mg),\n`pantothenic_acid`(mg), `vitamin_b6`(mg), `biotin`(mcg), `vitamin_b12`(mcg),\n`vitamin_c`(mg), `vitamin_d`(mcg), `vitamin_e`(mg), `vitamin_k`(mcg),\n`folate`(mcg), `calcium`(mg), `chloride`(mg), `iron`(mg), `magnesium`(mg),\n`phosphorus`(mg), `potassium`(mg), `zinc`(mg), `copper`(mg), `manganese`(mg),\n`molybdenum`(mcg), `selenium`(mcg), `chromium`(mcg), `iodine`(mcg).\n",
    "commits": [
      {
        "hash": "f2dc0c3",
        "date": "2026-06-01",
        "message": "Merge pull request #1 from C-Nucifora/log-meal-to-health"
      },
      {
        "hash": "5d830cc",
        "date": "2026-05-29",
        "message": "Add Log Meal to Health shortcut"
      },
      {
        "hash": "ba940d3",
        "date": "2026-05-29",
        "message": "Add implementation plan for Log Meal to Health shortcut"
      },
      {
        "hash": "dac698f",
        "date": "2026-05-29",
        "message": "Clarify Claude only logs nutrients it is confident in"
      },
      {
        "hash": "5175111",
        "date": "2026-05-29",
        "message": "Add design spec for Log Meal to Health shortcut"
      },
      {
        "hash": "8e8f930",
        "date": "2026-05-26",
        "message": "Add Rickroll Text shortcut"
      },
      {
        "hash": "a451930",
        "date": "2026-05-26",
        "message": "Add first two shortcuts: Bee Movie Spam and Unsigncuts for macOS"
      },
      {
        "hash": "4f813bf",
        "date": "2026-05-26",
        "message": "Scaffold repo for version-controlled Shortcuts backup"
      }
    ]
  },
  {
    "slug": "instagram-unliker",
    "title": "instagram-unliker",
    "pitch": "Python project — see the README.",
    "stack": [
      "Python"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/instagram-unliker",
    "featured": false,
    "stars": 1,
    "pushedAt": "2026-05-26",
    "readme": "# unliker\n\nSystematically unlike all of your Instagram likes via the private mobile API\n(using [instagrapi](https://github.com/subzeroid/instagrapi)).\n\n## Install\n\n```sh\npip install -r requirements.txt\n```\n\n## Configure\n\nEither set environment variables:\n\n```sh\nexport IG_USERNAME=your_username\nexport IG_PASSWORD=your_password\n# optional, if 2FA is enabled and you want non-interactive runs:\nexport IG_2FA_CODE=123456\n```\n\n…or put the same keys in a JSON config file and pass it with `--config`:\n\n```json\n{\n    \"IG_USERNAME\": \"your_username\",\n    \"IG_PASSWORD\": \"your_password\",\n    \"IG_2FA_CODE\": \"123456\"\n}\n```\n\nEnvironment variables take precedence over values from the config file.\n\n## Usage\n\n```sh\npython unlike.py                    # process all likes\npython unlike.py --dry-run          # list likes without unliking\npython unlike.py --limit 50         # only unlike 50 posts this run\npython unlike.py --config cfg.json  # load credentials from a JSON file\n```\n\n## Notes\n\n- Instagram aggressively rate-limits unlike actions. The script sleeps\n  between calls and pauses longer every batch to stay under the radar. If you\n  hit a \"please wait a few minutes\" error, it backs off for 15 minutes and\n  exits — just run it again later.\n- Session credentials are cached in `session.json` so 2FA / checkpoint\n  challenges only need to be solved once. This file is gitignored.\n- Progress is persisted in `unliked.json` so re-runs skip already-processed\n  media even if Instagram's liked-feed pagination shifts. Also gitignored.\n\n## Disclaimer\n\nUse at your own risk. Automating actions on Instagram violates their Terms of\nService and can result in your account being rate-limited, restricted, or\nbanned. This tool is provided for educational purposes.\n",
    "commits": [
      {
        "hash": "9970a0f",
        "date": "2026-05-26",
        "message": "Document --config flag in README"
      },
      {
        "hash": "fd6ee54",
        "date": "2026-05-26",
        "message": "Support loading credentials from a JSON config file"
      },
      {
        "hash": "560cbf7",
        "date": "2026-05-26",
        "message": "Run until done or interrupted, with verbose CLI logging"
      },
      {
        "hash": "5df7be0",
        "date": "2026-05-26",
        "message": "Initial commit: Instagram unliker script"
      }
    ]
  },
  {
    "slug": "leetcode",
    "title": "leetcode",
    "pitch": "Python project — see the README.",
    "stack": [
      "Python"
    ],
    "sourceUrl": "https://github.com/C-Nucifora/leetcode",
    "featured": false,
    "stars": 0,
    "pushedAt": "2026-03-30",
    "readme": "# LeetCode \nThis repository contains my solutions to LeetCode problems, primarily in Python.\n\nMy focus is on learning common problem solving patterns and writing clean implementations.\n\n## Progress\nProblems Solved: 7\n- Easy: 6\n- Medium: 1\n- Hard: 0\n\n\n## Structure\nFiles are named using the format:\n\n\\<id\\>_\\<slug\\>.py\n\nIDs are zero padded for consistent sorting.\n\nExample:\n01662_check_if_two_string_arrays_are_equivalent.py\n\n## Finding Problems\n\n- Search by problem number (e.g. `1662`)\n- Search by keyword (e.g. `two_sum`, `binary_search`)\n",
    "commits": [
      {
        "hash": "c765e3a",
        "date": "2026-03-30",
        "message": "Solved Problem 00009: Palindrome Number"
      },
      {
        "hash": "3e103b6",
        "date": "2026-03-30",
        "message": "Solved Problem 00001: Two Sum"
      },
      {
        "hash": "a599ddc",
        "date": "2026-03-28",
        "message": "Updated keyword formatting in several solutions"
      },
      {
        "hash": "bfd8632",
        "date": "2026-03-28",
        "message": "Fixed markdown syntax for Struture section of the Readme"
      },
      {
        "hash": "b67b695",
        "date": "2026-03-28",
        "message": "Updated the repo with all previous LeetCode solutions from ~2024. Also updated the Readme to reflect current progress."
      },
      {
        "hash": "cb713ed",
        "date": "2026-03-28",
        "message": "Initial commit"
      }
    ]
  }
];
